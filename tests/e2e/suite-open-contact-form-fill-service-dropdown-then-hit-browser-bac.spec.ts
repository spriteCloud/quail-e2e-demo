/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open contact form, fill service dropdown, then hit browser back before submit', async ({ page }) => {
  await opGoto(page, '/contact')
  await opClick(page, 'combobox', 'Which service are you interested in')
  await opPress(page, 'ArrowDown')
  await opPress(page, 'Enter')
  await opFill(page, 'Name', 'Back Button Prober')
  await opFill(page, 'Email', 'backprober@example.com')
  await opFill(page, 'Message', 'Testing browser navigation mid-form')
  await opPress(page, 'Backspace')
  await opClick(page, 'link', 'Contact')
})
