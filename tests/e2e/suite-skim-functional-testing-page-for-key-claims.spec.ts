/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Skim functional testing page for key claims', async ({ page }) => {
  await opGoto(page, '/functional-testing')
  await opSeen(page, 'heading', 'Functional Testing')
  await opSeen(page, 'heading', 'Why choose spriteCloud for functional testing?')
  await opSeen(page, 'heading', 'Our functional testing process')
  await opClick(page, 'button', 'Testing Services')
  await opSeen(page, 'heading', 'Functional Testing')
  await opSeen(page, 'heading', 'Agentic Testing')
  await opSeen(page, 'heading', 'Cybersecurity & Penetration Testing')
})
