/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Engaging with upcoming QA community events', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'Barcelona IT Community Meetup May 2026')
  await opClick(page, 'button', 'Register now')
  await opSeen(page, 'dialog', 'Event registration')
  await opFill(page, 'Email', 'visitor@example.com')
  await opPress(page, 'Enter')
})
