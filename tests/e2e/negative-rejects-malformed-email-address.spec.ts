/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:negative @smoke Rejects malformed email address', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Your Name', 'Alex Johnson')
  await opFill(page, 'Work Email', 'not-an-email')
  await opFill(page, 'Company Name', 'Tech Innovations Inc.')
  await opFill(page, 'Phone Number', '+1 (555) 123-4567')
  await opFill(page, 'What are you interested in?', 'QA automation services')
  await opFill(page, 'What\'s the situation?', 'We need help scaling our test automation for a new product launch.')
  await opClick(page, 'button', 'Send Message')
  await opSeen(page, 'dialog', 'Please enter a valid email address')
})
