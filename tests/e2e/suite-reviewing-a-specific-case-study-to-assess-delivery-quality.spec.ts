/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Reviewing a specific case study to assess delivery quality', async ({ page }) => {
  await opGoto(page, '/case-study/oikocredit')
  await opClick(page, 'link', 'Test Automation')
  await opClick(page, 'link', 'Oikocredit — Case Study — spriteCloud')
  await opSeen(page, 'heading', 'Oikocredit')
})
