import { test, expect } from '@playwright/test'

/**
 * Generated by quail.
 *
 * Integration-layer AUTH SCAFFOLD for https://www.spritecloud.com. Emitted when the
 * probe observed WWW-Authenticate or required Authorization headers
 * on at least one response. Skipped until the consumer declares an
 * auth provider in quail.yml.
 *
 * To activate:
 *   1. Declare an `auth:` block in quail.yml (kind: oauth2 / oidc /
 *      basic; issuer; client_id; scopes).
 *   2. Set QUAIL_AUTH_TOKEN at runtime, OR declare a token-mint
 *      strategy.
 *   3. Re-run `quail generate`.
 *
 * Filter: `npx playwright test --grep @kind:integration-auth`
 */
test.describe('spriteCloud — integration: authentication scaffold (skipped)', () => {
  test.skip('@kind:integration-auth @wireable @unauth: missing Authorization header returns 401', async ({ request }) => {
    // When wired:
    //   1. Probe a protected endpoint without Authorization
    //   2. Confirm 401 (not 403) — 401 signals "needs auth" properly
    //   3. Confirm WWW-Authenticate header points at the issuer
    const r = await request.get('/', { failOnStatusCode: false })
    expect.soft(r.status()).toBe(401)
  })

  test.skip('@kind:integration-auth @wireable @valid-token: valid token returns 2xx', async ({ request }) => {
    // When wired:
    //   1. Mint a token (or read QUAIL_AUTH_TOKEN)
    //   2. Request with Authorization: Bearer <token>
    //   3. Confirm 2xx response shape includes the expected resource
    const token = process.env.QUAIL_AUTH_TOKEN || ''
    if (!token) test.skip()
    const r = await request.get('/', {
      headers: { Authorization: `Bearer ${token}` },
    })
    expect(r.ok()).toBeTruthy()
  })

  test.skip('@kind:integration-auth @wireable @expired: expired token is rejected', async ({ request }) => {
    // When wired:
    //   1. Mint a token with exp in the past, OR use an obviously bad
    //      token from QUAIL_AUTH_TOKEN_EXPIRED
    //   2. Request → expect 401 (NOT 200; NOT 5xx)
    //   3. Confirm response body does NOT echo the token
    const r = await request.get('/', {
      headers: { Authorization: 'Bearer expired.token.placeholder' },
      failOnStatusCode: false,
    })
    expect.soft(r.status()).toBe(401)
    const body = await r.text().catch(() => '')
    expect.soft(body).not.toContain('expired.token.placeholder')
  })
})

/**
 * Generated by quail.
 *
 * Integration-layer AUTH SCAFFOLD for https://www.spritecloud.com. Emitted when the
 * probe observed WWW-Authenticate or required Authorization headers
 * on at least one response. Skipped until the consumer declares an
 * auth provider in quail.yml.
 *
 * To activate:
 *   1. Declare an `auth:` block in quail.yml (kind: oauth2 / oidc /
 *      basic; issuer; client_id; scopes).
 *   2. Set QUAIL_AUTH_TOKEN at runtime, OR declare a token-mint
 *      strategy.
 *   3. Re-run `quail generate`.
 *
 * Filter: `npx playwright test --grep @kind:integration-auth`
 */
test.describe('Spritecloud Integration Auth Scaffold (Skipped)', () => {
  test.skip('Missing Authorization returns 401', async ({ request }) => {
    // When wired:
    //   1. Probe a protected endpoint without Authorization
    //   2. Confirm 401 (not 403) — 401 signals "needs auth" properly
    //   3. Confirm WWW-Authenticate header points at the issuer
    const r = await request.get('/', { failOnStatusCode: false })
    expect.soft(r.status()).toBe(401)
  })

  test.skip('Valid token Authorization returns 2xx', async ({ request }) => {
    // When wired:
    //   1. Mint a token (or read QUAIL_AUTH_TOKEN)
    //   2. Request with Authorization: Bearer <token>
    //   3. Confirm 2xx response shape includes the expected resource
    const token = process.env.QUAIL_AUTH_TOKEN || ''
    if (!token) test.skip()
    const r = await request.get('/', {
      headers: { Authorization: `Bearer ${token}` },
    })
    expect(r.ok()).toBeTruthy()
  })

  test.skip('Expired token Authorization is rejected', async ({ request }) => {
    // When wired:
    //   1. Mint a token with exp in the past, OR use an obviously bad
    //      token from QUAIL_AUTH_TOKEN_EXPIRED
    //   2. Request → expect 401 (NOT 200; NOT 5xx)
    //   3. Confirm response body does NOT echo the token
    const r = await request.get('/', {
      headers: { Authorization: 'Bearer expired.token.placeholder' },
      failOnStatusCode: false,
    })
    expect.soft(r.status()).toBe(401)
    const body = await r.text().catch(() => '')
    expect.soft(body).not.toContain('expired.token.placeholder')
  })
})

/**
 * Generated by quail.
 *
 * Integration-layer AUTH SCAFFOLD for https://www.spritecloud.com. Emitted when the
 * probe observed WWW-Authenticate or required Authorization headers
 * on at least one response. Skipped until the consumer declares an
 * auth provider in quail.yml.
 *
 * To activate:
 *   1. Declare an `auth:` block in quail.yml (kind: oauth2 / oidc /
 *      basic; issuer; client_id; scopes).
 *   2. Set QUAIL_AUTH_TOKEN at runtime, OR declare a token-mint
 *      strategy.
 *   3. Re-run `quail generate`.
 *
 * Filter: `npx playwright test --grep @kind:integration-auth`
 */
test.describe('Spritecloud — integration auth scaffold (skipped)', () => {
  test.skip('integration auth, unauthenticated requests return 401', async ({ request }) => {
    // When wired:
    //   1. Probe a protected endpoint without Authorization
    //   2. Confirm 401 (not 403) — 401 signals "needs auth" properly
    //   3. Confirm WWW-Authenticate header points at the issuer
    const r = await request.get('/', { failOnStatusCode: false })
    expect.soft(r.status()).toBe(401)
  })

  test.skip('integration auth, valid token authorization returns 2xx', async ({ request }) => {
    // When wired:
    //   1. Mint a token (or read QUAIL_AUTH_TOKEN)
    //   2. Request with Authorization: Bearer <token>
    //   3. Confirm 2xx response shape includes the expected resource
    const token = process.env.QUAIL_AUTH_TOKEN || ''
    if (!token) test.skip()
    const r = await request.get('/', {
      headers: { Authorization: `Bearer ${token}` },
    })
    expect(r.ok()).toBeTruthy()
  })

  test.skip('integration auth, expired token authorization is rejected', async ({ request }) => {
    // When wired:
    //   1. Mint a token with exp in the past, OR use an obviously bad
    //      token from QUAIL_AUTH_TOKEN_EXPIRED
    //   2. Request → expect 401 (NOT 200; NOT 5xx)
    //   3. Confirm response body does NOT echo the token
    const r = await request.get('/', {
      headers: { Authorization: 'Bearer expired.token.placeholder' },
      failOnStatusCode: false,
    })
    expect.soft(r.status()).toBe(401)
    const body = await r.text().catch(() => '')
    expect.soft(body).not.toContain('expired.token.placeholder')
  })
})

/**
 * Generated by quail.
 *
 * Integration-layer AUTH SCAFFOLD for https://www.spritecloud.com. Emitted when the
 * probe observed WWW-Authenticate or required Authorization headers
 * on at least one response. Skipped until the consumer declares an
 * auth provider in quail.yml.
 *
 * To activate:
 *   1. Declare an `auth:` block in quail.yml (kind: oauth2 / oidc /
 *      basic; issuer; client_id; scopes).
 *   2. Set QUAIL_AUTH_TOKEN at runtime, OR declare a token-mint
 *      strategy.
 *   3. Re-run `quail generate`.
 *
 * Filter: `npx playwright test --grep @kind:integration-auth`
 */
test.describe('Spritecloud — authentication scaffold for integration (skipped)', () => {
  test.skip('unauthenticated request without Authorization returns 401', async ({ request }) => {
    // When wired:
    //   1. Probe a protected endpoint without Authorization
    //   2. Confirm 401 (not 403) — 401 signals "needs auth" properly
    //   3. Confirm WWW-Authenticate header points at the issuer
    const r = await request.get('/', { failOnStatusCode: false })
    expect.soft(r.status()).toBe(401)
  })

  test.skip('request with valid token returns 2xx', async ({ request }) => {
    // When wired:
    //   1. Mint a token (or read QUAIL_AUTH_TOKEN)
    //   2. Request with Authorization: Bearer <token>
    //   3. Confirm 2xx response shape includes the expected resource
    const token = process.env.QUAIL_AUTH_TOKEN || ''
    if (!token) test.skip()
    const r = await request.get('/', {
      headers: { Authorization: `Bearer ${token}` },
    })
    expect(r.ok()).toBeTruthy()
  })

  test.skip('request with expired token is rejected with 401', async ({ request }) => {
    // When wired:
    //   1. Mint a token with exp in the past, OR use an obviously bad
    //      token from QUAIL_AUTH_TOKEN_EXPIRED
    //   2. Request → expect 401 (NOT 200; NOT 5xx)
    //   3. Confirm response body does NOT echo the token
    const r = await request.get('/', {
      headers: { Authorization: 'Bearer expired.token.placeholder' },
      failOnStatusCode: false,
    })
    expect.soft(r.status()).toBe(401)
    const body = await r.text().catch(() => '')
    expect.soft(body).not.toContain('expired.token.placeholder')
  })
})

/**
 * Generated by quail.
 *
 * Integration-layer AUTH SCAFFOLD for https://www.spritecloud.com. Emitted when the
 * probe observed WWW-Authenticate or required Authorization headers
 * on at least one response. Skipped until the consumer declares an
 * auth provider in quail.yml.
 *
 * To activate:
 *   1. Declare an `auth:` block in quail.yml (kind: oauth2 / oidc /
 *      basic; issuer; client_id; scopes).
 *   2. Set QUAIL_AUTH_TOKEN at runtime, OR declare a token-mint
 *      strategy.
 *   3. Re-run `quail generate`.
 *
 * Filter: `npx playwright test --grep @kind:integration-auth`
 */
test.describe('Spritecloud — integration auth scaffold (skipped)', () => {
  test.skip('unauthenticated request (no Authorization header) returns HTTP 401', async ({ request }) => {
    // When wired:
    //   1. Probe a protected endpoint without Authorization
    //   2. Confirm 401 (not 403) — 401 signals "needs auth" properly
    //   3. Confirm WWW-Authenticate header points at the issuer
    const r = await request.get('/', { failOnStatusCode: false })
    expect.soft(r.status()).toBe(401)
  })

  test.skip('valid Authorization token grants access (returns 2xx)', async ({ request }) => {
    // When wired:
    //   1. Mint a token (or read QUAIL_AUTH_TOKEN)
    //   2. Request with Authorization: Bearer <token>
    //   3. Confirm 2xx response shape includes the expected resource
    const token = process.env.QUAIL_AUTH_TOKEN || ''
    if (!token) test.skip()
    const r = await request.get('/', {
      headers: { Authorization: `Bearer ${token}` },
    })
    expect(r.ok()).toBeTruthy()
  })

  test.skip('expired Authorization token is rejected (returns 401)', async ({ request }) => {
    // When wired:
    //   1. Mint a token with exp in the past, OR use an obviously bad
    //      token from QUAIL_AUTH_TOKEN_EXPIRED
    //   2. Request → expect 401 (NOT 200; NOT 5xx)
    //   3. Confirm response body does NOT echo the token
    const r = await request.get('/', {
      headers: { Authorization: 'Bearer expired.token.placeholder' },
      failOnStatusCode: false,
    })
    expect.soft(r.status()).toBe(401)
    const body = await r.text().catch(() => '')
    expect.soft(body).not.toContain('expired.token.placeholder')
  })
})

/**
 * Generated by quail.
 *
 * Integration-layer AUTH SCAFFOLD for https://www.spritecloud.com. Emitted when the
 * probe observed WWW-Authenticate or required Authorization headers
 * on at least one response. Skipped until the consumer declares an
 * auth provider in quail.yml.
 *
 * To activate:
 *   1. Declare an `auth:` block in quail.yml (kind: oauth2 / oidc /
 *      basic; issuer; client_id; scopes).
 *   2. Set QUAIL_AUTH_TOKEN at runtime, OR declare a token-mint
 *      strategy.
 *   3. Re-run `quail generate`.
 *
 * Filter: `npx playwright test --grep @kind:integration-auth`
 */
test.describe('Spritecloud — integration auth scaffold (skipped)', () => {
  test.skip('unauthenticated requests return 401 when authorization is missing', async ({ request }) => {
    // When wired:
    //   1. Probe a protected endpoint without Authorization
    //   2. Confirm 401 (not 403) — 401 signals "needs auth" properly
    //   3. Confirm WWW-Authenticate header points at the issuer
    const r = await request.get('/', { failOnStatusCode: false })
    expect.soft(r.status()).toBe(401)
  })

  test.skip('valid tokens return 2xx responses', async ({ request }) => {
    // When wired:
    //   1. Mint a token (or read QUAIL_AUTH_TOKEN)
    //   2. Request with Authorization: Bearer <token>
    //   3. Confirm 2xx response shape includes the expected resource
    const token = process.env.QUAIL_AUTH_TOKEN || ''
    if (!token) test.skip()
    const r = await request.get('/', {
      headers: { Authorization: `Bearer ${token}` },
    })
    expect(r.ok()).toBeTruthy()
  })

  test.skip('expired tokens are rejected with 401', async ({ request }) => {
    // When wired:
    //   1. Mint a token with exp in the past, OR use an obviously bad
    //      token from QUAIL_AUTH_TOKEN_EXPIRED
    //   2. Request → expect 401 (NOT 200; NOT 5xx)
    //   3. Confirm response body does NOT echo the token
    const r = await request.get('/', {
      headers: { Authorization: 'Bearer expired.token.placeholder' },
      failOnStatusCode: false,
    })
    expect.soft(r.status()).toBe(401)
    const body = await r.text().catch(() => '')
    expect.soft(body).not.toContain('expired.token.placeholder')
  })
})
