/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Filter case studies by search term for performance testing relevance', async ({ page }) => {
  await opGoto(page, '/work')
  await opFill(page, 'Search', 'performance')
  await opPress(page, 'Enter')
  await opClick(page, 'link', 'eCommerce platform — Case Study')
  await opSeen(page, 'heading', 'eCommerce platform — Case Study')
})
