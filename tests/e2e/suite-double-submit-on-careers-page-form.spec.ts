/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Double-submit on careers page form', async ({ page }) => {
  await opGoto(page, '/careers')
  await opFill(page, 'First Name', 'Adversary')
  await opPress(page, 'Tab')
  await opFill(page, 'Email', 'fast@double.submit')
  await opClick(page, 'button', 'Submit')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'dialog', 'Submission received')
})
