/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Rapid double-click on submit inside event modal form', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'button', 'Register')
  await opClick(page, 'button', 'Register')
  await opFill(page, 'Name', 'Test User')
  await opFill(page, 'Email', 'test@example.com')
  await opPress(page, 'Enter')
  await opSeen(page, 'dialog', 'Registration confirmed')
})
