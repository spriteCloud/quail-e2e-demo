/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:coverage @smoke Visit work page and expand testing accordion', async ({ page }) => {
  await opGoto(page, '/work')
  await opClick(page, 'button', 'Testing Services')
  await opSeen(page, 'heading', 'Work')
})
