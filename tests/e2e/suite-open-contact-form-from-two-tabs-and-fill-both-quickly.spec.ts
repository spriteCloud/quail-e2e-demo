/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open contact form from two tabs and fill both quickly', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'First Name', 'Jordan')
  await opFill(page, 'Last Name', 'Kim')
  await opFill(page, 'Email', 'j.kim@test.com')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opSeen(page, 'button', 'Send')
})
