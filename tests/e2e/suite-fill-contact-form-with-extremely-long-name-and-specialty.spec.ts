/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill contact form with extremely long name and specialty', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Dr. Professor Dr. Professor Dr. Professor Dr. Professor Dr. Professor Dr. Professor Dr. Professor Dr. Professor Dr. Professor Dr. Professor Dr. Professor Dr. Professor Dr. Professor Dr. Professor Dr. Professor')
  await opFill(page, 'Email', 'test@example.com')
  await opFill(page, 'Message', 'A message that is intentionally very long to test text input handling, wrapping, validation, and database truncation. This message continues with more text, more words, more punctuation, and more filler to ensure the field does not crash or misbehave under load.')
  await opClick(page, 'button', 'Submit')
})
