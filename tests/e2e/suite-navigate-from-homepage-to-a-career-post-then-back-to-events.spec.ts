/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke  Navigate from homepage to a career post, then back to events', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Company')
  await opClick(page, 'link', 'Careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'link', 'Testing Services')
  await opClick(page, 'link', 'Events & Meetups')
  await opSeen(page, 'heading', 'Events & Meetups')
})
