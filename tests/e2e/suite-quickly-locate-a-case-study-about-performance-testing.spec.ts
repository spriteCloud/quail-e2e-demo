/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Quickly locate a case study about performance testing', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Work')
  await opSeen(page, 'main', 'Work')
  await opClick(page, 'link', 'ecommerce platform')
  await opSeen(page, 'heading', 'E-commerce performance testing')
})
