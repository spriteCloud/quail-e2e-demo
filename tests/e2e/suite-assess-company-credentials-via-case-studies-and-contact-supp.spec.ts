/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Assess company credentials via case studies and contact support', async ({ page }) => {
  await opGoto(page, '/case-study/oikocredit')
  await opClick(page, 'link', 'Case Studies')
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Jordan')
  await opFill(page, 'Email', 'jordan@testops.io')
  await opFill(page, 'Message', 'Interested in a custom performance testing audit')
  await opClick(page, 'combobox', 'Which service are you interested in')
  await opPress(page, 'ArrowDown')
  await opPress(page, 'Enter')
  await opClick(page, 'button', 'Send message')
})
