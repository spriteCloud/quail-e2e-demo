/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Pre-select service in contact form via navigation route', async ({ page }) => {
  await opGoto(page, '/test-automation')
  await opClick(page, 'link', 'Contact')
  await opSeen(page, 'combobox', 'Which service are you interested in')
})
