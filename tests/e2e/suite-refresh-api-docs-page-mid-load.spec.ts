/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Refresh API docs page mid-load', async ({ page }) => {
  await opGoto(page, '/')
  await opPress(page, 'F5')
  await opSeen(page, 'heading', 'Swagger UI')
})
