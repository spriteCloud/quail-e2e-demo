/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:coverage @smoke Agentic testing service page visit', async ({ page }) => {
  await opGoto(page, '/agentic-testing')
  await opSeen(page, 'main', 'Agentic Testing — spriteCloud')
})
