/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Back-button abuse during event registration flow', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'QA Community Meetup April 2026')
  await opClick(page, 'button', 'Register interest')
  await opFill(page, 'Email', 'test@example.com')
  await opPress(page, 'Tab')
  await opPress(page, 'Backspace')
  await opClick(page, 'button', 'Register interest')
  await opSeen(page, 'dialog', 'Form submitted')
})
