/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Skim functional testing page content', async ({ page }) => {
  await opGoto(page, '/functional-testing')
  await opSeen(page, 'heading', 'Functional Testing')
  await opSeen(page, 'link', 'Testing Services')
  await opSeen(page, 'link', 'Test Automation')
  await opSeen(page, 'link', 'Performance Testing')
})
