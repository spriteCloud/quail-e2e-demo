/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Skim case study list for performance testing proof', async ({ page }) => {
  await opGoto(page, '/work')
  await opClick(page, 'heading', 'Work')
  await opClick(page, 'link', 'PostNL')
  await opSeen(page, 'heading', 'PostNL')
  await opClick(page, 'link', 'Performance Testing')
})
