/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Discover company values through About and contact page flow', async ({ page }) => {
  await opGoto(page, '/about')
  await opClick(page, 'link', 'Contact')
  await opFill(page, 'Which service are you interested in', 'General inquiry')
  await opSeen(page, 'heading', 'Contact')
})
