/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Check case study layout and structure', async ({ page }) => {
  await opGoto(page, '/case-study/postnl')
  await opSeen(page, 'heading', 'PostNL')
  await opClick(page, 'link', 'Functional Testing')
  await opSeen(page, 'heading', 'Functional Testing')
})
