/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open multiple case studies in background tabs then return', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Case Study')
  await opClick(page, 'link', 'Oikocredit')
  await opPress(page, 'Control')
  await opClick(page, 'link', 'PostNL')
  await opPress(page, 'Control')
  await opClick(page, 'link', 'citizenM')
  await opSeen(page, 'main', 'spriteCloud')
})
