/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Quick check of current job openings', async ({ page }) => {
  await opGoto(page, '/careers')
  await opSeen(page, 'heading', 'Careers')
  await opSeen(page, 'link', 'Test Automation Engineer')
  await opSeen(page, 'link', 'HR Internship – Process & People')
})
