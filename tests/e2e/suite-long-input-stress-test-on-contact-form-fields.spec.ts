/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Long-input stress test on contact form fields', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'A very long name that exceeds typical input validation thresholds — this name contains fifty-one characters')
  await opFill(page, 'Email', 'this-email-is-extremely-long-and-may-trigger-validation-or-truncation-in-the-backend@example-domain-with-a-really-long-subdomain-prefix.test.com')
  await opFill(page, 'Which service are you interested in', 'Functional Testing')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Thank you')
})
