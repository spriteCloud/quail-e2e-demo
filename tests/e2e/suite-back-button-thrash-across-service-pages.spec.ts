/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Back-button thrash across service pages', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Test Automation')
  await opClick(page, 'link', 'Functional Testing')
  await opPress(page, 'Back')
  await opPress(page, 'Back')
  await opPress(page, 'Back')
  await opSeen(page, 'heading', 'QA & Software Testing Consultancy — spriteCloud')
})
