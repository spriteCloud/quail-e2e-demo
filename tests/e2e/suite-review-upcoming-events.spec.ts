/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Review upcoming events', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Events & Meetups')
  await opSeen(page, 'heading', 'Events & Meetups')
  await opClick(page, 'link', 'QA Community Meetup April 2026')
  await opSeen(page, 'heading', 'DIAG-formtrue-pastfalse-ctatrue')
})
