/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Compare two case studies side-by-side via navigation', async ({ page }) => {
  await opGoto(page, '/work')
  await opClick(page, 'link', 'PostNL')
  await opClick(page, 'link', 'Testing Services')
  await opClick(page, 'link', 'Test Automation')
  await opClick(page, 'link', 'citizenM')
  await opSeen(page, 'heading', 'citizenM')
})
