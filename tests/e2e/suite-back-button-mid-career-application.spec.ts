/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Back-button mid-career application', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'button', 'View all vacancies')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'button', 'Apply now')
  await opFill(page, 'Name', 'Jane Smith')
  await opFill(page, 'Email', 'jane@example.com')
  await opPress(page, 'Back')
  await opSeen(page, 'link', 'View all vacancies')
})
