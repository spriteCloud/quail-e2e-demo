/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Check one event detail page directly', async ({ page }) => {
  await opGoto(page, '/event/qa-meetup-november-2025')
  await opSeen(page, 'heading', 'QA Meetup November 2025')
  await opClick(page, 'button', 'Register interest')
  await opSeen(page, 'dialog', 'Event registration form')
})
