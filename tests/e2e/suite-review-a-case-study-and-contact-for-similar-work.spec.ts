/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Review a case study and contact for similar work', async ({ page }) => {
  await opGoto(page, '/work')
  await opClick(page, 'link', 'Oikocredit')
  await opSeen(page, 'heading', 'Oikocredit')
  await opClick(page, 'link', 'Contact')
  await opClick(page, 'combobox', 'Which service are you interested in')
  await opFill(page, 'Which service are you interested in', 'Functional Testing')
  await opSeen(page, 'heading', 'Contact us')
})
