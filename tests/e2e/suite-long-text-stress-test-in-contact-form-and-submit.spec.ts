/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Long-text stress test in contact form and submit', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA')
  await opFill(page, 'Email', 'x@x')
  await opFill(page, 'Message', 'This is a very long message. ')
  await opPress(page, 'Enter')
  await opFill(page, 'Message', 'Continuing the long message with extra filler to exceed likely server-side limits and observe graceful handling. Adding more text. ')
  await opPress(page, 'Enter')
  await opSeen(page, 'dialog', 'Message sent')
})
