/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Check case study with performance testing focus', async ({ page }) => {
  await opGoto(page, '/case-study/ecommerce-performance')
  await opSeen(page, 'heading', 'eCommerce platform')
  await opSeen(page, 'heading', 'Challenge')
  await opSeen(page, 'heading', 'Solution')
  await opSeen(page, 'heading', 'Results')
})
