/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke View all upcoming events and register for the first one', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'QA community meetup April 2026')
  await opSeen(page, 'dialog', 'Event details')
})
