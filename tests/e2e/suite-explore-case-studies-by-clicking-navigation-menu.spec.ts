/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Explore case studies by clicking navigation menu', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Work')
  await opClick(page, 'link', 'Oikocredit')
  await opClick(page, 'link', 'PostNL')
  await opClick(page, 'link', 'citizenM')
  await opSeen(page, 'heading', 'citizenM — Case Study')
})
