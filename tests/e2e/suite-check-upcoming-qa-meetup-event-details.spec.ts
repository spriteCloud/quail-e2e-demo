/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Check upcoming QA meetup event details', async ({ page }) => {
  await opGoto(page, '/events')
  await opSeen(page, 'heading', 'Upcoming Events')
  await opSeen(page, 'heading', 'QA Community Meetup — April 2026')
  await opSeen(page, 'button', 'Register')
})
