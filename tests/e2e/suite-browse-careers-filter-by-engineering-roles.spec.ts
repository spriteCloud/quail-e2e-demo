/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Browse careers, filter by engineering roles', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'link', 'HR Internship')
  await opClick(page, 'link', 'Careers')
  await opSeen(page, 'main', 'Careers — spriteCloud')
})
