/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill form then browser back mid-request', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'button', 'Try it out')
  await opFill(page, 'status', 'available')
  await opClick(page, 'button', 'Execute')
  await opPress(page, 'Backspace')
})
