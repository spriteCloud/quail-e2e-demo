/**
 * Generated by quail from the bug discovery ledger.
 *
 * Bug SENTINEL — this spec is intentionally marked as failing (`test.fail()`). It will
 * report as an expected failure while the underlying bug stays unfixed,
 * and pivot to an unexpected pass the moment the fix is deployed. That
 * pivot is your signal to remove this sentinel marker and graduate it to
 * a regular regression test.
 *
 *   - Bug:     @kind:touch @smoke long-press on the primary CTA does not double-fire
 *   - Spec:    tests/e2e/touch/performance-testing.touch.spec.ts
 *   - Symptom: Error: browserType.launch:
 *   - Severity: medium
 *   - First seen: 2026-06-20
 *
 * Run with: `npx playwright test --grep @kind:sentinel`
 */
import { test, expect } from '@playwright/test'

test.fail('@kind:sentinel @severity:medium @kind:touch @smoke long-press on the primary CTA does not double-fire — this test fails until the bug is fixed', async ({ page }) => {
  // This test reproduces the original failing scenario to serve as a sentinel. The tool quail
  // encapsulates the symptom in the assertion below; the test remains
  // marked as failing (`test.fail()`) until the bug is resolved.
  await page.goto('/')
  // Original symptom from the bug ledger:
  //   Error: browserType.launch:
  expect(`Error: browserType.launch:`).toBe('') // if bug is present, this string is non-empty, causing failure
})
