/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Register for next upcoming event in one flow', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'Event')
  await opFill(page, 'First Name', 'Alex')
  await opFill(page, 'Last Name', 'Tester')
  await opFill(page, 'Email', 'alex.tester@example.com')
  await opPress(page, 'Enter')
})
