/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Refresh contact page mid-fill, then submit', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Neo')
  await opFill(page, 'Email', 'neo@matrix.net')
  await opPress(page, 'F5')
  await opSeen(page, 'heading', 'Contact Us')
  await opFill(page, 'Message', 'After refresh, still submitting')
})
