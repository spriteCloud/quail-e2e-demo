/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:coverage @smoke Visit functional testing page and expand Company accordion', async ({ page }) => {
  await opGoto(page, '/functional-testing')
  await opClick(page, 'menuitem', 'Company')
  await opSeen(page, 'heading', 'About spriteCloud')
})
