/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Submit a contact inquiry for performance testing services', async ({ page }) => {
  await opGoto(page, '/contact')
  await opClick(page, 'combobox', 'Which service are you interested in')
  await opClick(page, 'option', 'Performance Testing')
  await opFill(page, 'Name', 'Jordan Smith')
  await opFill(page, 'Email', 'j.smith@company.com')
  await opFill(page, 'Message', 'We need load testing for our e-commerce platform launch.')
  await opPress(page, 'Enter')
})
