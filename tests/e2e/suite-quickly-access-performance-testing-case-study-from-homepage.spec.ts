/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Quickly access performance testing case study from homepage', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Performance Testing')
  await opClick(page, 'link', 'eCommerce platform')
  await opSeen(page, 'heading', 'eCommerce platform — Case Study')
})
