/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke View contact form with pre-filtered service', async ({ page }) => {
  await opGoto(page, '/contact')
  await opClick(page, 'button', 'Which service are you interested in')
  await opClick(page, 'option', 'Test Automation')
  await opSeen(page, 'heading', 'Contact')
})
