/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Enter extremely long text in contact service filter dropdown', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Functional TestingTest AutomationPerformance TestingCybersecurityAgentic TestingTesting ServicesTesting ServicesTesting Services')
  await opPress(page, 'Enter')
  await opSeen(page, 'combobox', 'Which service are you interested in')
})
