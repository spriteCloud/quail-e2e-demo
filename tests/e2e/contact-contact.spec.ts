/* quail quality report
 * Weak / missing locators on this page:
 *   - no data-testid attributes found on this page (0 inputs, 4 anchors). Tests rely on text/role and break under copy edits.
 * Add data-testid to these elements for stable tests.
 */
/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:composed @smoke Fill contact form and submit', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'First name', 'Alex')
  await opFill(page, 'Last name', 'Johnson')
  await opFill(page, 'Email', 'alex.johnson@example.com')
  await opFill(page, 'Company', 'TechNova Inc.')
  await opFill(page, 'What are you interested in?', 'Test automation')
  await opFill(page, 'What\'s the situation?', 'Looking to automate our regression suite for a new web app.')
  await opClick(page, 'button', 'Send inquiry')
})
