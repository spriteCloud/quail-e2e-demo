/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Browse open roles via careers page', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'menuitem', 'Company')
  await opSeen(page, 'link', 'Test Automation Engineer')
  await opSeen(page, 'link', 'HR Internship')
})
