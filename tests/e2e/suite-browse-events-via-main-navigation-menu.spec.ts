/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Browse events via main navigation menu', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Events')
  await opSeen(page, 'heading', 'Events & Meetups')
  await opClick(page, 'link', 'QA Community Meetup April 2026')
})
