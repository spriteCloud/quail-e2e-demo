/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Double-click submit on contact form after fill', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Test User')
  await opFill(page, 'Email', 'test@example.com')
  await opFill(page, 'Message', 'Inquiry about performance testing for high-load systems.')
  await opPress(page, 'Tab')
  await opPress(page, 'Tab')
  await opPress(page, 'Tab')
  await opClick(page, 'button', 'Send')
  await opClick(page, 'button', 'Send')
  await opSeen(page, 'main', 'Contact')
})
