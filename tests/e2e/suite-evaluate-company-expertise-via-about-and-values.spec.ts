/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Evaluate company expertise via about and values', async ({ page }) => {
  await opGoto(page, '/about')
  await opClick(page, 'link', 'About')
  await opSeen(page, 'heading', 'About spriteCloud')
  await opClick(page, 'link', 'Testing Services')
  await opSeen(page, 'heading', 'Testing Services')
})
