/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:negative @smoke rejects empty email field', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'button', 'Apply Now')
  await opFill(page, 'Full Name', 'Jordan Lee')
  await opFill(page, 'Email', '')
  await opClick(page, 'button', 'Submit Application')
  await opSeen(page, 'alert', 'Email is required')
})
