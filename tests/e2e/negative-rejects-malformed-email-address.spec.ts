/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:negative @smoke Rejects malformed email address', async ({ page }) => {
  await opGoto(page, '/contact')
  await opClick(page, 'combobox', 'Which service are you interested in')
  await opClick(page, 'option', 'Test Automation')
  await opFill(page, 'Name', 'Alex')
  await opFill(page, 'Email', 'not-an-email')
  await opFill(page, 'Message', 'Interested in automating our regression suite.')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'dialog', 'Please enter a valid email address.')
})
