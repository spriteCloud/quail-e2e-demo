/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Review career opportunities and apply', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'link', 'Apply for this position')
  await opFill(page, 'Email', 'dev@example.com')
  await opFill(page, 'Name', 'Dev Candidate')
  await opSeen(page, 'button', 'Submit application')
})
