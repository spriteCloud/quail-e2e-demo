/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke From homepage to event form to register for upcoming meetup', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Events & Meetups')
  await opClick(page, 'link', 'QA Community Meetup April 2026')
  await opFill(page, 'Name', 'Alex')
  await opFill(page, 'Email', 'alex@example.com')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Thank you')
})
