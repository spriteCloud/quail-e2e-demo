/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Filter contact form for performance testing inquiry', async ({ page }) => {
  await opGoto(page, '/contact')
  await opSeen(page, 'combobox', 'Which service are you interested in')
  await opClick(page, 'combobox', 'Which service are you interested in')
  await opClick(page, 'option', 'Performance Testing')
  await opSeen(page, 'heading', 'Contact')
})
