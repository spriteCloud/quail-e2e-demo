/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:negative @smoke rejects malformed email in contact form', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Alex Johnson')
  await opFill(page, 'Email', 'not-an-email')
  await opFill(page, 'Company', 'TechCorp')
  await opFill(page, 'Phone', '+1-555-123-4567')
  await opFill(page, 'What are you interested in?', 'Test Automation')
  await opFill(page, 'What\'s the situation?', 'We\'re building a new web app and need automated regression testing.')
  await opClick(page, 'button', 'Send message')
  await opSeen(page, 'alert', 'Please enter a valid email address')
})
