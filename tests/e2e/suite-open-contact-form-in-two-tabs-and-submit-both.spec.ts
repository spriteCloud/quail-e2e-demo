/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open contact form in two tabs and submit both', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Performance Testing')
  await opFill(page, 'Name', 'Multiuser')
  await opFill(page, 'Email', 'multi@tab.com')
  await opClick(page, 'button', 'Submit')
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Performance Testing')
  await opFill(page, 'Name', 'Multiuser')
  await opFill(page, 'Email', 'multi@tab.com')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'dialog', 'Thank you for contacting us')
})
