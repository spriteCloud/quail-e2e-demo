/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open contact page in two tabs and submit both rapidly', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Name', 'Alex Dev')
  await opFill(page, 'Email', 'alex@example.com')
  await opFill(page, 'Message', 'Automation audit requested.')
  await opClick(page, 'button', 'Send message')
  await opClick(page, 'button', 'Send message')
})
