/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke View all case studies, then filter by service via breadcrumb', async ({ page }) => {
  await opGoto(page, '/work')
  await opClick(page, 'link', 'All case studies')
  await opSeen(page, 'heading', 'Case Studies')
  await opClick(page, 'link', 'Test Automation')
  await opSeen(page, 'heading', 'Test Automation')
  await opClick(page, 'link', 'Back to all case studies')
  await opSeen(page, 'heading', 'Case Studies')
})
