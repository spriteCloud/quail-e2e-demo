/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Filter contact form by service interest', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Performance Testing')
  await opFill(page, 'Name', 'Alex Smith')
  await opFill(page, 'Email', 'alex@example.com')
  await opFill(page, 'Message', 'Request demo for load testing of APIs.')
  await opClick(page, 'button', 'Send message')
  await opSeen(page, 'heading', 'Thank You')
})
