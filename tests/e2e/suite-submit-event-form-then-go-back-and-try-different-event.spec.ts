/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Submit event form, then go back and try different event', async ({ page }) => {
  await opGoto(page, '/event/barcelona-it-community-meetup-may-2026')
  await opFill(page, 'First Name', 'Alex')
  await opFill(page, 'Last Name', 'Rivera')
  await opFill(page, 'Email', 'alex.rivera@example.com')
  await opPress(page, 'Enter')
  await opClick(page, 'link', 'Events')
  await opSeen(page, 'link', 'Event')
})
