/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Skims main page for test automation benefits', async ({ page }) => {
  await opGoto(page, '/')
  await opSeen(page, 'heading', 'Test Automation')
  await opClick(page, 'link', 'Test Automation')
  await opSeen(page, 'heading', 'Test Automation')
})
