/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Explore testing options and request a consultation', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Testing Services')
  await opClick(page, 'link', 'Test Automation')
  await opClick(page, 'button', 'Company')
  await opClick(page, 'link', 'Contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Name', 'Maria Chen')
  await opFill(page, 'Email', 'maria.chen@example.com')
  await opFill(page, 'Message', 'We’re evaluating test automation for our web app and would like a consult.')
  await opClick(page, 'button', 'Send')
})
