/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Backtrack during event registration mid-flow', async ({ page }) => {
  await opGoto(page, '/event/barcelona-it-community-meetup-may-2026')
  await opClick(page, 'button', 'Register')
  await opFill(page, 'First name', 'Alex')
  await opPress(page, 'Tab')
  await opFill(page, 'Last name', 'Smith')
  await opPress(page, 'Backspace')
  await opClick(page, 'button', 'Back')
  await opSeen(page, 'link', 'Register')
})
