/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Rapidly compare multiple case studies', async ({ page }) => {
  await opGoto(page, '/work')
  await opClick(page, 'link', 'Oikocredit')
  await opPress(page, 'Backspace')
  await opPress(page, 'Backspace')
  await opClick(page, 'link', 'PostNL')
  await opClick(page, 'link', 'Ben')
  await opSeen(page, 'heading', 'Ben')
})
