/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Refresh event page mid-form fill to check state persistence', async ({ page }) => {
  await opGoto(page, '/event/barcelona-it-community-meetup-may-2026')
  await opFill(page, 'Name', 'Alice')
  await opFill(page, 'Email', 'alice@example.com')
  await opPress(page, 'Tab')
  await opPress(page, 'Tab')
  await opPress(page, 'Tab')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Thank you! We\'ll be in touch shortly')
})
