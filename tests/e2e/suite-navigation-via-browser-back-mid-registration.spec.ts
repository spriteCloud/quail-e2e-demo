/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Navigation via browser back mid-registration', async ({ page }) => {
  await opGoto(page, '/event/qa-community-meetup-april-2026')
  await opFill(page, 'Name', 'John Smith')
  await opFill(page, 'Email', 'john@example.com')
  await opClick(page, 'button', 'Register now')
  await opPress(page, 'Backspace')
  await opSeen(page, 'heading', 'QA Community Meetup — April 2026')
})
