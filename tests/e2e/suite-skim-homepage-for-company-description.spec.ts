/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Skim homepage for company description', async ({ page }) => {
  await opGoto(page, '/')
  await opSeen(page, 'main', 'QA & Software Testing Consultancy')
  await opSeen(page, 'heading', ' spriteCloud helps teams build better software through expert QA and testing.')
})
