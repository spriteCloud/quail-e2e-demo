/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Read security testing content and reach out via contact form', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Cybersecurity & Penetration Testing')
  await opClick(page, 'link', 'Contact')
  await opFill(page, 'Which service are you interested in', 'Cybersecurity & Penetration Testing')
  await opFill(page, 'Name', 'Alex Johnson')
  await opFill(page, 'Email', 'alex@example.com')
  await opFill(page, 'Message', 'Interested in cybersecurity assessment for our web app.')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Thank you')
})
