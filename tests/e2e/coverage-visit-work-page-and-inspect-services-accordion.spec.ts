/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:coverage @smoke Visit Work page and inspect services accordion', async ({ page }) => {
  await opGoto(page, '/work')
  await opClick(page, 'menuitem', 'Testing Services')
  await opSeen(page, 'heading', 'Work')
})
