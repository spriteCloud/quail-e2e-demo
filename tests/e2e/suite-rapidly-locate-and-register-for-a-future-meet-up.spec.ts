/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Rapidly locate and register for a future meet-up', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'QA Community Meetup April 2026')
  await opClick(page, 'button', 'Register')
  await opFill(page, 'Your Name', 'Alex Tester')
  await opFill(page, 'Email', 'alex@example.com')
  await opPress(page, 'Enter')
})
