/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Consider joining the team by exploring open roles', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opSeen(page, 'main', 'Test Automation Engineer — Careers — spriteCloud')
  await opClick(page, 'link', 'HR Internship')
  await opSeen(page, 'main', 'HR Internship – Process & People — Careers — spriteCloud')
})
