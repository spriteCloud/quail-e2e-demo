/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Confirm agentic testing page content validity', async ({ page }) => {
  await opGoto(page, '/agentic-testing')
  await opSeen(page, 'heading', 'Agentic Testing')
  await opSeen(page, 'link', 'Functional Testing')
  await opClick(page, 'link', 'Functional Testing')
})
