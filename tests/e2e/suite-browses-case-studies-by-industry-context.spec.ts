/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Browses case studies by industry context', async ({ page }) => {
  await opGoto(page, '/work')
  await opClick(page, 'link', 'Oikocredit')
  await opSeen(page, 'heading', 'Oikocredit — Case Study')
  await opClick(page, 'link', 'PostNL')
  await opSeen(page, 'heading', 'PostNL — Case Study')
  await opClick(page, 'link', 'Case Study')
  await opGoto(page, '/case-study/citizenm')
  await opSeen(page, 'heading', 'citizenM — Case Study')
})
