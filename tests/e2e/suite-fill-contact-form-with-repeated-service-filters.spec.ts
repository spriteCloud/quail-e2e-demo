/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill contact form with repeated service filters', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Which service are you interested in', 'Functional Testing')
  await opPress(page, 'Tab')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'dialog', 'Error or success confirmation')
})
