/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Submit a contact inquiry about functional testing', async ({ page }) => {
  await opGoto(page, '/contact')
  await opClick(page, 'combobox', 'Which service are you interested in')
  await opClick(page, 'option', 'Functional Testing')
  await opFill(page, 'Name', 'Jordan Smith')
  await opFill(page, 'Email', 'jordan@company.com')
  await opFill(page, 'Message', 'We need help improving our regression coverage.')
  await opPress(page, 'Enter')
})
