/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Switching from events to case studies using footer navigation', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'Case Studies')
  await opSeen(page, 'link', 'Oikocredit')
})
