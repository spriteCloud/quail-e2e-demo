/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Attend a QA community meet-up', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Events & Meetups')
  await opClick(page, 'link', 'QA Community Meetup April 2026')
  await opClick(page, 'link', 'Register now')
  await opFill(page, 'Name', 'Riley Smith')
  await opFill(page, 'Email', 'riley@example.com')
  await opClick(page, 'button', 'Register')
})
