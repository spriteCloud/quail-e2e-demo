/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Plan attendance at a QA community meetup', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'QA community meetup April 2026')
  await opSeen(page, 'heading', 'QA Community Meetup April 2026')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Email', 'test@example.com')
})
