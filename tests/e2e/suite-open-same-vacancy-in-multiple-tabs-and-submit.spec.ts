/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Open same vacancy in multiple tabs and submit', async ({ page }) => {
  await opGoto(page, '/vacancy/test-automation-engineer')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opFill(page, 'Name', 'Multi-Tab User')
  await opFill(page, 'Email', 'multi@test.com')
  await opClick(page, 'button', 'Submit')
})
