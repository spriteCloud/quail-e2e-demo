/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Filter case studies by service interest', async ({ page }) => {
  await opGoto(page, '/work')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opPress(page, 'Enter')
})
