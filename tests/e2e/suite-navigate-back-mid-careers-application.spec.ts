/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Navigate back mid-careers application', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'HR Internship')
  await opClick(page, 'button', 'Apply Now')
  await opPress(page, 'Escape')
  await opClick(page, 'button', 'Back')
})
