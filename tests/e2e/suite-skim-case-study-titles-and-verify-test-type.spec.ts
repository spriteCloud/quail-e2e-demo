/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Skim case study titles and verify test type', async ({ page }) => {
  await opGoto(page, '/work')
  await opSeen(page, 'heading', 'Case Studies')
  await opSeen(page, 'heading', 'Oikocredit')
  await opSeen(page, 'heading', 'PostNL')
  await opSeen(page, 'heading', 'CitizenM')
})
