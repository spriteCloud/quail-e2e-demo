/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Back-button mid registration flow', async ({ page }) => {
  await opGoto(page, '/event/barcelona-it-community-meetup-may-2026')
  await opFill(page, 'Name', 'Jane')
  await opFill(page, 'Email', 'jane@test.com')
  await opClick(page, 'button', 'Register')
  await opPress(page, 'Backspace')
  await opClick(page, 'link', 'Back')
  await opSeen(page, 'heading', 'Barcelona IT Community Meetup May 2026')
})
