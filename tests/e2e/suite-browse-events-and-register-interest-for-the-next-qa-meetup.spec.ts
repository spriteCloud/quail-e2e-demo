/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Browse events and register interest for the next QA meetup', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'QA Community Meetup April 2026')
  await opClick(page, 'button', 'Register now')
  await opFill(page, 'First name', 'Alex')
  await opFill(page, 'Last name', 'Chen')
  await opFill(page, 'Email', 'alex.chen@example.com')
  await opSeen(page, 'heading', 'Thank you for registering')
})
