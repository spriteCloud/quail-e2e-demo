/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:negative @smoke rejects malformed email in test automation inquiry', async ({ page }) => {
  await opGoto(page, '/test-automation')
  await opClick(page, 'button', 'Get started')
  await opFill(page, 'Name', 'Alex Tester')
  await opFill(page, 'Email', 'not-an-email')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'alert', 'Please enter a valid email address')
})
