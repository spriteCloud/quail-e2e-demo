/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:coverage @smoke Open Company menu and inspect items', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'button', 'Company')
  await opSeen(page, 'heading', 'Company')
})
