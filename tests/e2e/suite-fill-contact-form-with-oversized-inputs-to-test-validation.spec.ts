/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill contact form with oversized inputs to test validation', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Name', 'A very long name that exceeds typical validation limits and should trigger proper error handling or truncation in the backend')
  await opFill(page, 'Email', 'verylongemailaddresswithmassiveamountsofcharacters@reallylongdomainnamethatshouldexceedtypicalvalidationlimits.com')
  await opFill(page, 'Message', 'This is a test message with an extremely long body that contains many words repeated over and over to ensure that the message field accepts and handles input lengths above normal expectations. We want to verify that no truncation errors, UI breakage, or backend rejections occur due to length. This is part of an adversarial probing effort. Thank you.')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'main', 'Thanks for contacting spriteCloud')
})
