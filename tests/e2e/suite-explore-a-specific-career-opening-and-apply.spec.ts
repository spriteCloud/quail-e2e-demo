/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Explore a specific career opening and apply', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'button', 'Apply now')
  await opFill(page, 'Name', 'Jordan Dev')
  await opFill(page, 'Email', 'jordan@example.com')
  await opClick(page, 'button', 'Submit')
})
