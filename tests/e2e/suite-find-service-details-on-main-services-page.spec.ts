/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Find service details on main Services page', async ({ page }) => {
  await opGoto(page, '/services')
  await opClick(page, 'heading', 'Functional Testing')
  await opSeen(page, 'heading', 'Functional Testing')
  await opClick(page, 'heading', 'Test Automation')
  await opSeen(page, 'heading', 'Test Automation')
})
