/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill contact form then refresh mid-entry', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Test User')
  await opFill(page, 'Email', 'test@example.com')
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'New User')
  await opSeen(page, 'button', 'Submit')
})
