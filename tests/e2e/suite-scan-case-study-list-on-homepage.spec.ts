/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Scan case study list on homepage', async ({ page }) => {
  await opGoto(page, '/')
  await opSeen(page, 'link', 'Oikocredit')
  await opSeen(page, 'link', 'PostNL')
  await opSeen(page, 'link', 'citizenM')
  await opSeen(page, 'link', 'GiftShift')
  await opSeen(page, 'link', 'Ben')
  await opSeen(page, 'link', 'eCommerce platform')
})
