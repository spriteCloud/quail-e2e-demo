/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Compare test automation and performance testing pages', async ({ page }) => {
  await opGoto(page, '/test-automation')
  await opSeen(page, 'heading', 'Test Automation — spriteCloud')
  await opGoto(page, '/performance-testing')
  await opSeen(page, 'heading', 'Performance Testing — spriteCloud')
})
