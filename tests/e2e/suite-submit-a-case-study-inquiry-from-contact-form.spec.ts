/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Submit a case study inquiry from contact form', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Name', 'Alex Rivera')
  await opFill(page, 'Email', 'alex.rivera@example.com')
  await opFill(page, 'Message', 'We would like to submit a case study for review.')
  await opClick(page, 'button', 'Submit')
})
