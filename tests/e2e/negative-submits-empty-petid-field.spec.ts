/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:negative @smoke submits empty petId field', async ({ page }) => {
  await opGoto(page, '/api/v3/openapi.json')
  await opSeen(page, 'main', 'Swagger Petstore')
  await opFill(page, 'petId', '')
  await opClick(page, 'button', 'Try it out')
  await opSeen(page, 'alert', 'petId is required')
})
