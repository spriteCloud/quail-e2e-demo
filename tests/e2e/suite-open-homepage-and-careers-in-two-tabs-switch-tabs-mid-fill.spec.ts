/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open homepage and careers in two tabs, switch tabs mid-fill', async ({ page }) => {
  await opGoto(page, '/')
  await opGoto(page, '/careers')
  await opFill(page, 'Name', 'Alex')
  await opPress(page, 'Tab')
  await opFill(page, 'Email', 'alex@example.com')
  await opClick(page, 'button', 'Submit')
})
