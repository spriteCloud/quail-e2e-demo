/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Check career openings via Quick Link', async ({ page }) => {
  await opGoto(page, '/careers')
  await opSeen(page, 'main', 'Careers')
  await opClick(page, 'link', 'HR Internship – Process & People')
  await opSeen(page, 'main', 'HR Internship – Process & People')
})
