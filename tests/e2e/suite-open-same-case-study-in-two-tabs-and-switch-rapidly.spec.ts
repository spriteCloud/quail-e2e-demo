/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open same case study in two tabs and switch rapidly', async ({ page }) => {
  await opGoto(page, '/case-study/citizenm')
  await opClick(page, 'link', 'Home')
  await opClick(page, 'link', 'Case Study: citizenM')
  await opSeen(page, 'heading', 'citizenM — Case Study — spriteCloud')
})
