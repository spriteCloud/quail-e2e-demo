/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Refresh contact page after service filter selection', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'John')
  await opFill(page, 'Email', 'john@example.com')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opPress(page, 'Tab')
  await opSeen(page, 'combobox', 'Which service are you interested in')
})
