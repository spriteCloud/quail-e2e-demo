/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:negative @smoke Submit event registration with empty name and email', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Events & Meetups')
  await opClick(page, 'link', 'QA Community Meetup April 2026')
  await opFill(page, 'Email', '')
  await opClick(page, 'button', 'Register')
  await opSeen(page, 'alert', 'Name and email are required')
})
