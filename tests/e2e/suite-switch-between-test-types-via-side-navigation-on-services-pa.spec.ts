/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Switch between test types via side navigation on services pages', async ({ page }) => {
  await opGoto(page, '/performance-testing')
  await opClick(page, 'link', 'Functional Testing')
  await opClick(page, 'link', 'Cybersecurity & Penetration Testing')
  await opClick(page, 'link', 'Agentic Testing')
  await opSeen(page, 'heading', 'Agentic Testing')
})
