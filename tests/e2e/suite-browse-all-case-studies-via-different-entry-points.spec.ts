/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Browse all case studies via different entry points', async ({ page }) => {
  await opGoto(page, '/work')
  await opClick(page, 'link', 'Case Studies')
  await opClick(page, 'link', 'Oikocredit')
  await opClick(page, 'link', 'PostNL')
  await opPress(page, 'BackSpace')
  await opSeen(page, 'link', 'Oikocredit')
  await opClick(page, 'link', 'citizenM')
  await opSeen(page, 'heading', 'citizenM')
})
