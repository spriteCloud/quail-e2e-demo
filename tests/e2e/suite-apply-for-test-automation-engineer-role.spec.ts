/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Apply for test automation engineer role', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opFill(page, 'Name', 'Maria Chen')
  await opFill(page, 'Email', 'maria.chen@example.com')
  await opFill(page, 'LinkedIn', 'linkedin.com/in/mariachen')
  await opClick(page, 'button', 'Apply Now')
  await opSeen(page, 'heading', 'Your application has been received')
})
