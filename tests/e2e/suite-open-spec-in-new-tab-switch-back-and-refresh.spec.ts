/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open spec in new tab, switch back, and refresh', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Swagger JSON')
  await opSeen(page, 'main', 'Swagger Petstore')
  await opClick(page, 'button', 'Swagger UI')
  await opSeen(page, 'main', 'Swagger Petstore')
  await opPress(page, 'F5')
})
