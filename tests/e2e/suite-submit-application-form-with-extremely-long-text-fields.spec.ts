/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Submit application form with extremely long text fields', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'button', 'Apply now')
  await opFill(page, 'Your name', 'A very long name ')
  await opFill(page, 'Email address', 'user@example.com')
  await opFill(page, 'Message', 'This is an extremely long message that might exceed normal input limits and is intended to test how the form handles overflow, special characters, and validation. ')
  await opFill(page, 'Message', 'Continuation of the extremely long message to ensure field truncation or error handling is tested thoroughly—this may trigger client-side or server-side limits. ')
  await opPress(page, 'Tab')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'dialog', 'Application received')
})
