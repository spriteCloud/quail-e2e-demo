/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Submit contact inquiry with service filter', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Email address', 'user@example.com')
  await opFill(page, 'Message', 'I\'m interested in your test automation offerings.')
  await opClick(page, 'button', 'Send message')
  await opSeen(page, 'heading', 'Thanks for getting in touch')
})
