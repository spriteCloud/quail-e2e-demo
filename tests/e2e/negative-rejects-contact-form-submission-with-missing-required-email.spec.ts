/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:negative @smoke rejects contact form submission with missing required email', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'First name', 'Alex')
  await opFill(page, 'Last name', 'Morgan')
  await opFill(page, 'Company', 'TechGlobal')
  await opFill(page, 'What are you interested in?', 'QA automation')
  await opFill(page, 'What\'s the situation?', 'We need a test strategy for a new product launch.')
  await opClick(page, 'button', 'Send message')
  await opSeen(page, 'dialog', 'Email is required')
})
