/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Race between form view and refresh after partial fill', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Performance Testing')
  await opFill(page, 'Name', 'R')
  await opPress(page, 'Tab')
  await opPress(page, 'F5')
  await opSeen(page, 'heading', 'Contact')
})
