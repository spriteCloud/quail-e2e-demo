/**
 * Generated by quail from the bug-discovery ledger.
 *
 * Bug SENTINEL — this spec is intentionally `test.fail()`. It will
 * report "expected to fail" while the underlying bug stays unfixed,
 * and pivot to "unexpected pass" the moment the fix lands. That
 * pivot is your signal to remove the sentinel and graduate it to
 * a regular regression test.
 *
 *   - Bug:     @smoke @heal-demo: spritecloud homepage shows the hero anchor
 *   - Spec:    tests/e2e/heal-demo/broken-locator.heal-demo.spec.ts
 *   - Symptom: Error: expect(locator).toBeVisible: locator resolved to 0 elements
 *   - Severity: low
 *   - First seen: 2026-07-03
 *
 * Filter: `npx playwright test --grep @kind:sentinel`
 */
import { test, expect } from '@playwright/test'

test.fail('@kind:sentinel @severity:low @smoke @heal-demo:.spritecloud homepage shows the hero anchor', async ({ page }) => {
  // Sentinel: reproduces the original failure. Quail bottles the symptom
  // into the assertion below; test stays `test.fail()` until the fix lands.
  await page.goto('/')
  // Symptom (from ledger):
  //   Error: expect(locator).toBeVisible: locator resolved to 0 elements
  expect('Error: expect(locator).toBeVisible: locator resolved to 0 elements').toBe('') // Bug present → non-empty string → test fails
})
