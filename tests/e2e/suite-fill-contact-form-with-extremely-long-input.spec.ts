/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Fill contact form with extremely long input', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'A very long name that exceeds typical field length limits and may trigger validation errors or truncation issues')
  await opFill(page, 'Email', 'a@b.c')
  await opFill(page, 'Message', 'Lorem ipsum dolor sit amet. ')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Contact')
})
