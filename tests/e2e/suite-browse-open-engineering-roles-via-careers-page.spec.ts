/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Browse open engineering roles via careers page', async ({ page }) => {
  await opGoto(page, '/careers')
  await opSeen(page, 'heading', 'Open Positions')
})
