/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:coverage @smoke Performance testing service page visit', async ({ page }) => {
  await opGoto(page, '/performance-testing')
  await opSeen(page, 'main', 'Performance Testing — spriteCloud')
})
