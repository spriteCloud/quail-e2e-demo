/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Submit careers form via browser back after filling', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'HR Internship')
  await opClick(page, 'button', 'Apply now')
  await opFill(page, 'Name', 'Eve')
  await opFill(page, 'Email', 'eve@example.com')
  await opClick(page, 'button', 'Submit')
  await opClick(page, 'button', 'Back')
  await opSeen(page, 'heading', 'Careers')
})
