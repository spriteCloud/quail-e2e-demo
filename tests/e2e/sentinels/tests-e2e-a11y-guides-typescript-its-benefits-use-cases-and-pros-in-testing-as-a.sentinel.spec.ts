/**
 * Generated by quail from the bug-discovery ledger.
 *
 * Bug SENTINEL — this spec is intentionally `test.fail()`. It will
 * report "expected to fail" while the underlying bug stays unfixed,
 * and pivot to "unexpected pass" the moment the fix lands. That
 * pivot is your signal to remove the sentinel and graduate it to
 * a regular regression test.
 *
 *   - Bug:     @kind:a11y-landmarks @smoke single main + h1 + nav
 *   - Spec:    tests/e2e/a11y/guides-typescript-its-benefits-use-cases-and-pros-in-testing-as-a-language.landmarks.spec.ts
 *   - Symptom: TimeoutError: page.goto: Timeout 20000ms exceeded.
 *   - Severity: medium
 *   - First seen: 2026-06-20
 *
 * Filter: `npx playwright test --grep @kind:sentinel`
 */
import { test, expect } from '@playwright/test'

test.fail('Sentinel: a11y landmark test fails with timeout on page.goto', async ({ page }) => {
  // This test reproduces the original timeout issue; remains failing until bug is fixed.
  await page.goto('/')
  // Original symptom: navigation takes too long (>20s)
  expect('').toBe('TimeoutError: page.goto: Timeout 20000ms exceeded.') // fail when bug is present
})
