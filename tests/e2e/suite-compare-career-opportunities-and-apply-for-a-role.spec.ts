/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Compare career opportunities and apply for a role', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'button', 'Apply Now')
  await opFill(page, 'Email', 'candidate@example.com')
  await opFill(page, 'CV or Resume', '/path/to/resume.pdf')
  await opSeen(page, 'dialog', 'Application submitted')
})
