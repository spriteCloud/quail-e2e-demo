/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Contact the team with a specific service interest', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Name', 'Alex Smith')
  await opFill(page, 'Email', 'alex@example.com')
  await opFill(page, 'Message', 'We\'re evaluating test automation partnerships')
  await opClick(page, 'button', 'Submit')
})
