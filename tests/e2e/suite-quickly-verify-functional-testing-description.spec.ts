/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Quickly verify functional testing description', async ({ page }) => {
  await opGoto(page, '/functional-testing')
  await opSeen(page, 'heading', 'Functional Testing')
  await opClick(page, 'link', 'Testing Services')
  await opSeen(page, 'heading', 'Testing Services')
})
