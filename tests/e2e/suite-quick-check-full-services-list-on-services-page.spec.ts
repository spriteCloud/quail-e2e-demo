/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Quick check full services list on services page', async ({ page }) => {
  await opGoto(page, '/services')
  await opSeen(page, 'link', 'Functional Testing')
  await opSeen(page, 'link', 'Test Automation')
  await opSeen(page, 'link', 'Performance Testing')
})
