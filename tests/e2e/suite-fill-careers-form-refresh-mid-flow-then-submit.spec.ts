/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill careers form, refresh mid-flow, then submit', async ({ page }) => {
  await opGoto(page, '/careers')
  await opFill(page, 'Name', 'Alex Tester')
  await opFill(page, 'Email', 'alex@protonmail.com')
  await opFill(page, 'CV Upload', '/tmp/fake-cv.pdf')
  await opPress(page, 'Tab')
  await opFill(page, 'Message', 'My application')
  await opGoto(page, '/careers')
  await opClick(page, 'button', 'Send application')
})
