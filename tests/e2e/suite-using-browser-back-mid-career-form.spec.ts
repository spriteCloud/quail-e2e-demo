/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Using browser back mid-career form', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opFill(page, 'Name', 'Alex')
  await opFill(page, 'Email', 'alex@example.com')
  await opPress(page, 'Tab')
  await opFill(page, 'LinkedIn profile', 'https://linkedin.com/alex')
  await opPress(page, 'Backspace')
  await opClick(page, 'button', 'Apply now')
  await opClick(page, 'link', 'Careers')
  await opSeen(page, 'heading', 'Careers')
})
