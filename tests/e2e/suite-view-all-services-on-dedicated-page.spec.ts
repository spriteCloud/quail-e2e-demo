/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke View all services on dedicated page', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Services')
  await opSeen(page, 'heading', 'Testing Services')
})
