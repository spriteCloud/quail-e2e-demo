/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:coverage @smoke Visit functional testing page and expand menu', async ({ page }) => {
  await opGoto(page, '/functional-testing')
  await opClick(page, 'menuitem', 'Testing Services')
  await opSeen(page, 'heading', 'Functional Testing')
})
