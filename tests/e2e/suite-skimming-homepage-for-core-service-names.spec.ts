/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Skimming homepage for core service names', async ({ page }) => {
  await opGoto(page, '/')
  await opSeen(page, 'heading', 'QA & Software Testing Consultancy')
  await opClick(page, 'link', 'Testing Services')
  await opSeen(page, 'link', 'Functional Testing')
  await opSeen(page, 'link', 'Test Automation')
  await opSeen(page, 'link', 'Performance Testing')
})
