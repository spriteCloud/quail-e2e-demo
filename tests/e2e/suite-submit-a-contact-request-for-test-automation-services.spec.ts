/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Submit a contact request for Test Automation services', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Full name', 'Alex Johnson')
  await opFill(page, 'Email address', 'alex@example.com')
  await opFill(page, 'Message', 'We’re evaluating test automation for our web app — can you help?')
  await opPress(page, 'Enter')
})
