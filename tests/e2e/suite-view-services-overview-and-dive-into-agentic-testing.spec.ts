/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke View services overview and dive into agentic testing', async ({ page }) => {
  await opGoto(page, '/services')
  await opClick(page, 'link', 'Agentic Testing')
  await opClick(page, 'link', 'Cybersecurity & Penetration Testing')
  await opClick(page, 'link', 'Work')
  await opSeen(page, 'main', 'Work')
})
