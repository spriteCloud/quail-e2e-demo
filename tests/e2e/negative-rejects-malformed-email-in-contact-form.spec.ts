/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:negative @smoke Rejects malformed email in contact form', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'First Name', 'Alex')
  await opFill(page, 'Last Name', 'Johnson')
  await opFill(page, 'Email', 'not-an-email')
  await opFill(page, 'Company', 'TechCorp')
  await opFill(page, 'What are you interested in?', 'Test Automation')
  await opFill(page, 'What\'s the situation?', 'We need help scaling our regression suite for a new CI/CD pipeline.')
  await opClick(page, 'button', 'Send')
  await opSeen(page, 'dialog', 'Please enter a valid email address')
})
