/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Race double-submit contact form', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Racer')
  await opFill(page, 'Email', 'racer@example.com')
  await opClick(page, 'button', 'Send')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Thanks for contacting us')
})
