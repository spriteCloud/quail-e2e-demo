/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Navigate tocontactform via services dropdown', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Testing Services')
  await opClick(page, 'link', 'Performance Testing')
  await opClick(page, 'link', 'Contact')
  await opSeen(page, 'heading', 'Contact')
})
