/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Check opening for test automation engineer via careers search', async ({ page }) => {
  await opGoto(page, '/careers')
  await opFill(page, 'Search by keyword', 'automation')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Test Automation Engineer')
})
