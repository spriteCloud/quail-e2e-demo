/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Compare case studies for performance testing outcomes', async ({ page }) => {
  await opGoto(page, '/performance-testing')
  await opClick(page, 'link', 'eCommerce platform')
  await opClick(page, 'link', 'spriteCloud')
  await opClick(page, 'link', 'PostNL')
  await opClick(page, 'link', 'spriteCloud')
  await opClick(page, 'link', 'spriteCloud')
  await opSeen(page, 'heading', 'spriteCloud')
})
