/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Browses company mission and team via About page', async ({ page }) => {
  await opGoto(page, '/about')
  await opClick(page, 'heading', 'About')
  await opSeen(page, 'main', 'About')
})
