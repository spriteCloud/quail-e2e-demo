/** Generated by quail from the bug-discovery ledger.
 *
 * Each test below is intentionally test.fail(). It reports
 * "expected to fail" while the tracked bug stays unfixed, and pivots
 * to "unexpected pass" the moment the fix lands — your signal to
 * delete the row from findings.md.
 *
 * Filter: npx playwright test --grep @kind:sentinel
 */
import { test, expect } from '@playwright/test'

test.describe('quail bug sentinels — from findings.md ledger', () => {
  test.fail("@kind:sentinel @severity:low tests/e2e/heal-demo/broken-locator.heal-demo.spec.ts — @smoke @heal-demo: spritecloud homepage shows the hero anchor", async ({ page }) => {
    // spec:    tests/e2e/heal-demo/broken-locator.heal-demo.spec.ts
    // test:    @smoke @heal-demo: spritecloud homepage shows the hero anchor
    // symptom: Error: expect(locator).toBeVisible: locator resolved to 0 elements
    // first seen: 2026-07-03
    await page.goto('/')
    expect("Error: expect(locator).toBeVisible: locator resolved to 0 elements").toBe('')
  })
})
