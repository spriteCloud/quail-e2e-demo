/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open contact page in two tabs, fill both, submit concurrently', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Multi-tab User')
  await opFill(page, 'Email', 'tab1@example.com')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Message', 'Tab 1 response')
  await opFill(page, 'Name', 'Multi-tab User')
  await opFill(page, 'Email', 'tab2@example.com')
  await opFill(page, 'Which service are you interested in', 'Functional Testing')
  await opFill(page, 'Message', 'Tab 2 response')
  await opClick(page, 'button', 'Send message')
})
