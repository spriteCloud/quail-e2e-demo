/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open two tabs to contact pages and race form submissions', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Performance Testing')
  await opFill(page, 'Name', 'Racer A')
  await opFill(page, 'Email', 'racer.a@example.com')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Thank you! We\'ll be in touch shortly')
})
