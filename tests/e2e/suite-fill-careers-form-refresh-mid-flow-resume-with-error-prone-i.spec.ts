/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill careers form, refresh mid-flow, resume with error-prone input', async ({ page }) => {
  await opGoto(page, '/careers')
  await opFill(page, 'Name', 'A')
  await opFill(page, 'Email', 'notanemail')
  await opPress(page, 'Tab')
  await opFill(page, 'Message', 'Testing refresh during fill')
  await opPress(page, 'F5')
  await opFill(page, 'Name', 'Jane Smith')
  await opFill(page, 'Email', 'jane@example.com')
  await opFill(page, 'Message', 'After refresh')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'main', 'Careers — spriteCloud')
})
