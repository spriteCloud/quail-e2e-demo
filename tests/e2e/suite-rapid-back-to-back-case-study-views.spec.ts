/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Rapid back-to-back case study views', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'PostNL')
  await opClick(page, 'link', 'Oikocredit')
  await opClick(page, 'link', 'citizenM')
  await opSeen(page, 'heading', 'citizenM')
})
