/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open contact page in two tabs, fill both, and submit rapidly', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Tab 1 User')
  await opFill(page, 'Email', 'tab1@example.com')
  await opFill(page, 'Message', 'Parallel tab submission test')
  await opClick(page, 'button', 'Send message')
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Tab 2 User')
  await opFill(page, 'Email', 'tab2@example.com')
  await opFill(page, 'Message', 'Second concurrent submission')
  await opClick(page, 'button', 'Send message')
})
