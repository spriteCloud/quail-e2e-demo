/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:coverage @smoke Homepage QA & Software Testing Consultancy visit', async ({ page }) => {
  await opGoto(page, '/')
  await opSeen(page, 'main', 'QA & Software Testing Consultancy — spriteCloud')
})
