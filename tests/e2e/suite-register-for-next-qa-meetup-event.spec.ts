/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Register for next QA meetup event', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'QA Community Meetup — April 2026')
  await opFill(page, 'First Name', 'Taylor')
  await opFill(page, 'Last Name', 'Kim')
  await opFill(page, 'Email', 'taylor@example.com')
  await opPress(page, 'Enter')
})
