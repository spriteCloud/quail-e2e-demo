/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open contact page in two tabs and submit both concurrently', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Performance Testing')
  await opFill(page, 'Name', 'Tab One')
  await opFill(page, 'Email', 'tab1@example.com')
  await opClick(page, 'button', 'Send message')
  await opSeen(page, 'dialog', 'Message sent confirmation')
})
