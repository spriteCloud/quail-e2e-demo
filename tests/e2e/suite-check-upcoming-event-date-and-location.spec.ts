/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Check upcoming event date and location', async ({ page }) => {
  await opGoto(page, '/event/barcelona-it-community-meetup-may-2026')
  await opSeen(page, 'heading', 'Barcelona IT Community Meetup')
  await opSeen(page, 'heading', 'May 2026')
  await opSeen(page, 'link', 'Testing Services')
  await opSeen(page, 'link', 'Functional Testing')
})
