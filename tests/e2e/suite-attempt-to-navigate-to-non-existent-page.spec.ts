/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Attempt to navigate to non-existent page', async ({ page }) => {
  await opGoto(page, '/nonexistent-page')
  await opSeen(page, 'main', 'Not Found')
})
