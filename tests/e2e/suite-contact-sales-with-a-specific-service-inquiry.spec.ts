/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Contact sales with a specific service inquiry', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Name', 'Alex Chen')
  await opFill(page, 'Email', 'alex@example.com')
  await opFill(page, 'Message', 'We\'re evaluating automation for our e-commerce platform.')
  await opClick(page, 'button', 'Send')
})
