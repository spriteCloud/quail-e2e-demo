/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Filter contact form by service interest', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Your Name', 'Alex Tester')
  await opFill(page, 'Your Email', 'alex@example.com')
  await opFill(page, 'Message', 'We’d like to discuss test automation for our e-commerce platform.')
  await opClick(page, 'button', 'Send')
  await opSeen(page, 'heading', 'Thank you for your message!')
})
