/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Check contact form service filter options', async ({ page }) => {
  await opGoto(page, '/contact')
  await opSeen(page, 'combobox', 'Which service are you interested in')
  await opClick(page, 'link', 'Performance Testing')
  await opClick(page, 'link', 'Test Automation')
  await opClick(page, 'link', 'Functional Testing')
})
