/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Preview service-specific contact flow', async ({ page }) => {
  await opGoto(page, '/contact')
  await opClick(page, 'combobox', 'Which service are you interested in')
  await opFill(page, 'Which service are you interested in', 'Functional Testing')
  await opSeen(page, 'dialog', 'Contact form — Functional Testing selected')
  await opSeen(page, 'textbox', 'Your message')
})
