/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Check contact form without selecting service', async ({ page }) => {
  await opGoto(page, '/contact')
  await opSeen(page, 'textbox', 'Your Name')
  await opFill(page, 'Your Name', 'Alex')
  await opFill(page, 'Email', 'alex@example.com')
  await opPress(page, 'Enter')
})
