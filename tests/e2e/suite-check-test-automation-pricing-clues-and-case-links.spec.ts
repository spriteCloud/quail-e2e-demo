/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Check test automation pricing clues and case links', async ({ page }) => {
  await opGoto(page, '/test-automation')
  await opSeen(page, 'heading', 'Test Automation')
  await opSeen(page, 'link', 'View all test automation case studies')
  await opSeen(page, 'link', 'Learn how we automate testing')
  await opSeen(page, 'heading', 'Key benefits')
})
