/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:negative @smoke Rejects empty contact form submission', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Testing Services')
  await opClick(page, 'link', 'Test Automation')
  await opClick(page, 'button', 'Company')
  await opClick(page, 'link', 'Contact')
  await opFill(page, 'Which service are you interested in', '')
  await opFill(page, 'Name', '')
  await opFill(page, 'Email', '')
  await opFill(page, 'Message', '')
  await opClick(page, 'button', 'Send')
  await opSeen(page, 'dialog', 'Please fill out the required fields')
})
