/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Navigate Careers from homepage → event → back via browser', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'Events')
  await opClick(page, 'link', 'Careers')
  await opClick(page, 'button', 'Back')
  await opClick(page, 'link', 'Events')
})
