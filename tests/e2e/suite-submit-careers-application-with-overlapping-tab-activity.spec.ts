/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Submit careers application with overlapping tab activity', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'HR Internship – Process & People')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opFill(page, 'Name', 'Avery')
  await opFill(page, 'Email', 'test@example.com')
  await opClick(page, 'button', 'Apply Now')
  await opSeen(page, 'dialog', 'Application submitted')
})
