/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Contact sales with vague interest using service filter', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Name', 'Alex Chen')
  await opFill(page, 'Email', 'alex@example.com')
  await opFill(page, 'Message', 'We’re exploring automation options')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Thank you')
})
