/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Quickly access career application from test automation page', async ({ page }) => {
  await opGoto(page, '/test-automation')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'link', 'Apply now')
  await opFill(page, 'Name', 'Alex Tester')
  await opFill(page, 'Email', 'alex@example.com')
  await opFill(page, 'Portfolio', 'https://alextest.dev')
  await opFill(page, 'Cover letter', 'Passionate about scalable test automation with 5+ years experience.')
  await opClick(page, 'button', 'Submit')
})
