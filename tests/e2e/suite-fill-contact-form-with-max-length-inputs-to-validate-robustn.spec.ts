/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Fill contact form with max-length inputs to validate robustness', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Your Name', 'A very long name that pushes the input boundaries and ensures the form handles overflow gracefully without errors')
  await opFill(page, 'Email', 'a@b.c')
  await opFill(page, 'Message', 'Test automation for legacy mainframe migration using modern tools. Need help with test design, CI/CD integration, flaky test reduction, and performance baselining over 12 months. Highly complex environment, multiple apps, strict compliance. Looking for vendor with deep domain knowledge and proven cases in financial services. Reach out soon for discovery call. Thanks very much for considering us for this project — let us know availability next week.')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'main', 'Contact – spriteCloud')
})
