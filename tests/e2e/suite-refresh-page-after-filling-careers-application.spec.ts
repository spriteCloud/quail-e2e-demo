/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Refresh page after filling careers application', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'button', 'Apply Now')
  await opFill(page, 'Full Name', 'Refreshed Candidate')
  await opFill(page, 'Email', 'refresh@test.com')
  await opPress(page, 'F5')
  await opSeen(page, 'heading', 'Test Automation Engineer')
})
