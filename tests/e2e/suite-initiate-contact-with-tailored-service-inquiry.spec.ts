/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Initiate contact with tailored service inquiry', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Contact')
  await opFill(page, 'Which service are you interested in', 'Functional Testing')
  await opFill(page, 'Name', 'Alex Developer')
  await opFill(page, 'Email', 'alex@example.com')
  await opFill(page, 'Message', 'We need help scaling our QA for a fintech product launch.')
  await opClick(page, 'button', 'Send')
})
