/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Check event details and register', async ({ page }) => {
  await opGoto(page, '/event/qa-meetup-april-2026')
  await opClick(page, 'link', 'Register now')
  await opFill(page, 'First Name', 'Jordan')
  await opFill(page, 'Last Name', 'Smith')
  await opFill(page, 'Email', 'jordansmith@example.com')
  await opClick(page, 'checkbox', 'I would like to receive updates about future events')
  await opSeen(page, 'dialog', 'Event registration')
})
