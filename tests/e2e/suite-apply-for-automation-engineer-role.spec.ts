/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Apply for automation engineer role', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'button', 'Apply now')
  await opFill(page, 'Full name', 'Jordan Tester')
  await opFill(page, 'Email', 'jordan@test.com')
  await opFill(page, 'LinkedIn', 'linkedin.com/in/jordantester')
  await opClick(page, 'button', 'Submit application')
  await opSeen(page, 'dialog', 'Application submitted')
})
