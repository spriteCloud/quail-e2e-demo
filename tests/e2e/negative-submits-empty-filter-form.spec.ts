/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:negative @smoke submits empty filter form', async ({ page }) => {
  await opGoto(page, '/careers')
  await opFill(page, 'Which service are you interested in', '')
  await opPress(page, 'Enter')
  await opSeen(page, 'alert', 'Please select a service')
})
