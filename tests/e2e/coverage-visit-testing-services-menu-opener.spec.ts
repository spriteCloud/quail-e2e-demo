/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:coverage @smoke Visit Testing Services menu opener', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'button', 'Testing Services')
  await opSeen(page, 'heading', 'Testing Services')
})
