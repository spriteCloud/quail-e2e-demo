/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Refresh contact page after partial form fill', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Test User')
  await opFill(page, 'Email', 'test@example.com')
  await opFill(page, 'Message', 'Testing form persistence')
  await opPress(page, 'Tab')
  await opPress(page, 'Tab')
  await opPress(page, 'F5')
  await opSeen(page, 'heading', 'Contact us')
})
