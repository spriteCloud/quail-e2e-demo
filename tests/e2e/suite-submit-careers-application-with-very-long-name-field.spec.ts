/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Submit careers application with very long name field', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opFill(page, 'Name', 'AlexandertheGreatAlexandertheGreatAlexandertheGreatAlexandertheGreatAlexandertheGreatAlexandertheGreat')
  await opFill(page, 'Email', 'test@example.com')
  await opFill(page, 'Message', 'My application text that\'s quite long but perfectly reasonable in a real application.')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'heading', 'Thank you for your application')
})
