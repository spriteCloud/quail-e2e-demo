/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open docs in two tabs and refresh second tab', async ({ page }) => {
  await opGoto(page, '/')
  await opPress(page, 'ControlOrMeta+w')
  await opGoto(page, '/')
  await opClick(page, 'button', 'Try it out')
  await opFill(page, 'petId', '1')
  await opClick(page, 'button', 'Execute')
  await opClick(page, 'button', 'Back')
  await opSeen(page, 'heading', 'POST /pet')
})
