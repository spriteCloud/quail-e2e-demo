/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:negative @smoke rejects malformed email', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'First name', 'Alex')
  await opFill(page, 'Last name', 'Roberts')
  await opFill(page, 'Email', 'notanemail')
  await opFill(page, 'What are you interested in?', 'QA consulting')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'alert', 'Please enter a valid email address')
})
