/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Submit a contact inquiry for test automation', async ({ page }) => {
  await opGoto(page, '/contact')
  await opClick(page, 'combobox', 'Which service are you interested in')
  await opClick(page, 'option', 'Test Automation')
  await opFill(page, 'First name', 'Jordan')
  await opFill(page, 'Last name', 'Lee')
  await opFill(page, 'Email', 'j.lee@company.com')
  await opFill(page, 'Company name', 'TechCorp')
  await opFill(page, 'Message', 'We need help scaling our test automation suite.')
  await opClick(page, 'button', 'Send message')
})
