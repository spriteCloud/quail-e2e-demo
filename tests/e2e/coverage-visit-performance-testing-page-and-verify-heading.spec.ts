/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:coverage @smoke Visit performance testing page and verify heading', async ({ page }) => {
  await opGoto(page, '/performance-testing')
  await opSeen(page, 'main', 'Performance Testing')
})
