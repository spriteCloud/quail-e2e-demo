/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:negative @smoke rejects malformed email address', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'First name', 'Alex')
  await opFill(page, 'Last name', 'Patel')
  await opFill(page, 'Email', 'not-an-email')
  await opFill(page, 'Phone', '+15551234567')
  await opFill(page, 'What are you interested in?', 'Test Automation')
  await opFill(page, 'What\'s the situation?', 'We need help.')
  await opClick(page, 'button', 'Send message')
  await opSeen(page, 'heading', 'Please fix invalid fields')
})
