/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill contact form, then refresh mid-flow', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Functional Testing')
  await opFill(page, 'Name', 'X')
  await opPress(page, 'Tab')
  await opFill(page, 'Email', 'longemailaddresswithmanycharacters@example.com')
  await opPress(page, 'F5')
  await opSeen(page, 'heading', 'Contact')
})
