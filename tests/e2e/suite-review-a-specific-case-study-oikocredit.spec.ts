/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Review a specific case study—Oikocredit', async ({ page }) => {
  await opGoto(page, '/case-study/oikocredit')
  await opSeen(page, 'heading', 'Oikocredit — Case Study — spriteCloud')
  await opClick(page, 'link', 'Case Studies')
  await opSeen(page, 'heading', 'Work — spriteCloud')
})
