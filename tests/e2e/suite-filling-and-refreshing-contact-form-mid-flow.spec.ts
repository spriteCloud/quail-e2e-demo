/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Filling and refreshing contact form mid-flow', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'John Doe')
  await opFill(page, 'Email', 'john@example.com')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opPress(page, 'Tab')
  await opPress(page, 'Enter')
  await opFill(page, 'Message', 'Before refresh')
  await opGoto(page, '/contact')
  await opSeen(page, 'heading', 'Contact')
})
