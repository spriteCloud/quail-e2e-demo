/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Quickly find and register for barcelona IT community meetup', async ({ page }) => {
  await opGoto(page, '/event/barcelona-it-community-meetup-may-2026')
  await opSeen(page, 'heading', 'DIAG-formtrue-pastfalse-ctatrue')
  await opClick(page, 'button', 'Register')
  await opFill(page, 'First Name', 'Alex')
  await opFill(page, 'Last Name', 'Smith')
  await opFill(page, 'Email', 'alex.smith@example.com')
  await opSeen(page, 'button', 'Submit')
})
