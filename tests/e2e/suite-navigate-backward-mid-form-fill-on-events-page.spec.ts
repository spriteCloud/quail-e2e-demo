/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Navigate backward mid-form fill on events page', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'QA Community Meetup April 2026')
  await opFill(page, 'Name', 'Backtracker')
  await opFill(page, 'Email', 'back@back.com')
  await opPress(page, 'BackSpace')
  await opPress(page, 'BackSpace')
  await opPress(page, 'BackSpace')
  await opPress(page, 'BackSpace')
  await opClick(page, 'button', 'Go back')
  await opSeen(page, 'link', 'Events &Meetups')
})
