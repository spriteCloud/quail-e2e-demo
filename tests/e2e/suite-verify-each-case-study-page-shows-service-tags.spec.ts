/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Verify each case study page shows service tags', async ({ page }) => {
  await opGoto(page, '/case-study/oikocredit')
  await opSeen(page, 'link', 'Testing Services')
  await opClick(page, 'link', 'Test Automation')
  await opSeen(page, 'heading', 'Test Automation')
})
