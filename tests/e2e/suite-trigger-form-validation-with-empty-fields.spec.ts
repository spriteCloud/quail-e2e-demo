/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Trigger form validation with empty fields', async ({ page }) => {
  await opGoto(page, '/contact')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'dialog', 'Error message about required fields')
})
