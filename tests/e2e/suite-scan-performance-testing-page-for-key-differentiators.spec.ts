/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Scan performance testing page for key differentiators', async ({ page }) => {
  await opGoto(page, '/performance-testing')
  await opSeen(page, 'heading', 'Performance Testing')
  await opClick(page, 'link', 'Test Automation')
  await opSeen(page, 'heading', 'Test Automation')
})
