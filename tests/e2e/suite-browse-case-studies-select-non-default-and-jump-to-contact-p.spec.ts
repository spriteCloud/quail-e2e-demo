/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Browse case studies, select non-default, and jump to contact page', async ({ page }) => {
  await opGoto(page, '/work')
  await opClick(page, 'link', 'GiftShift')
  await opClick(page, 'link', 'Contact')
  await opSeen(page, 'heading', 'Contact')
})
