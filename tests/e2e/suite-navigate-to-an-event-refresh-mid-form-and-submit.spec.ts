/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Navigate to an event, refresh mid-form, and submit', async ({ page }) => {
  await opGoto(page, '/event/qa-meetup-march-2025')
  await opFill(page, 'Name', 'Refreshed User')
  await opFill(page, 'Email', 'refresh@ex.com')
  await opClick(page, 'button', 'Register')
  await opPress(page, 'F5')
  await opSeen(page, 'heading', 'QA Community Meetup March 2025')
})
