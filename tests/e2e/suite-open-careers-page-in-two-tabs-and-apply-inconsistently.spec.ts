/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open careers page in two tabs and apply inconsistently', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opFill(page, 'Full name', 'Test User')
  await opFill(page, 'Email', 'test@example.com')
  await opFill(page, 'LinkedIn', 'https://linkedin.com/in/testuser')
  await opFill(page, 'Website', 'https://example.com')
  await opFill(page, 'Resume', '/path/to/resume.pdf')
  await opClick(page, 'button', 'Submit')
})
