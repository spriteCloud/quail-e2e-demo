/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Navigate via accordion menus and test browser back mid-flow', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Testing Services')
  await opClick(page, 'option', 'Test Automation')
  await opClick(page, 'option', 'Performance Testing')
  await opSeen(page, 'heading', 'Performance Testing')
})
