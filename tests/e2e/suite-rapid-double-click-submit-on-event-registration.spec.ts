/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Rapid double-click submit on event registration', async ({ page }) => {
  await opGoto(page, '/event/barcelona-it-community-meetup-may-2026')
  await opFill(page, 'First Name', 'Jane')
  await opFill(page, 'Last Name', 'Doe')
  await opFill(page, 'Email', 'jane@doe.com')
  await opClick(page, 'button', 'Register')
  await opClick(page, 'button', 'Register')
  await opSeen(page, 'dialog', 'Confirmation')
})
