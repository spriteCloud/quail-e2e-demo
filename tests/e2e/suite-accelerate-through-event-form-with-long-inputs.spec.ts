/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Accelerate through event form with long inputs', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'button', 'Register')
  await opFill(page, 'Name', 'ABCDEFGHIJKLMNOPQRSTUVWXYZABCDEFGHIJKLMNOPQRSTUVWXYZ')
  await opFill(page, 'Email', 'test@spritecloud.com')
  await opFill(page, 'Company', 'SpriteCloud SpriteCloud SpriteCloud SpriteCloud SpriteCloud')
  await opPress(page, 'Enter')
})
