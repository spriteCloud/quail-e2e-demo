/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Researcher reads case studies for performance validation', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Work')
  await opClick(page, 'link', 'eCommerce platform')
  await opClick(page, 'link', 'PostNL')
  await opClick(page, 'link', 'CitizenM')
  await opSeen(page, 'heading', 'citizenM')
})
