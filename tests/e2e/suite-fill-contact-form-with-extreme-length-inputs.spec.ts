/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill contact form with extreme-length inputs', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Functional Testing')
  await opFill(page, 'Name', 'AveryLongNameExceedingNormalExpectationsThatShouldStillBeAcceptedByTheForm')
  await opFill(page, 'Email', 'test@example.com')
  await opFill(page, 'Message', 'This is an extremely long message that includes lots of text to test how the form handles unusually large inputs, especially with special characters like quotes " and \' and line breaks.\nNew line here. Also testing Unicode: café résumé naïve')
  await opClick(page, 'button', 'Submit')
})
