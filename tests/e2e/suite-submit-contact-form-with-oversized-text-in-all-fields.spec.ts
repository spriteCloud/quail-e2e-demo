/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Submit contact form with oversized text in all fields', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Performance Testing')
  await opFill(page, 'Name', 'A very long name that exceeds typical input limits and may trigger validation or truncation — John Quasimodosuperduperlongname Doe')
  await opFill(page, 'Email', 'a')
})
