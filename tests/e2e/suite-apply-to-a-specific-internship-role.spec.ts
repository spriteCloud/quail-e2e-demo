/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Apply to a specific internship role', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'HR Internship – Process & People')
  await opFill(page, 'Name', 'Alex Qa')
  await opFill(page, 'Email', 'alex@example.com')
  await opClick(page, 'button', 'Apply Now')
})
