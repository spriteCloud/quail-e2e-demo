/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Back-button mid-event registration', async ({ page }) => {
  await opGoto(page, '/event/qa-community-meetup-april-2026')
  await opClick(page, 'button', 'Register')
  await opFill(page, 'Name', 'John Doe')
  await opPress(page, 'Backspace')
  await opClick(page, 'button', 'Back')
  await opSeen(page, 'main', 'QA Community Meetup April 2026')
})
