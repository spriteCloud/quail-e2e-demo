/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Register for a QA community meetup', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Events & Meetups')
  await opClick(page, 'link', 'QA Community Meetup April 2026')
  await opClick(page, 'button', 'Register now')
  await opFill(page, 'First name', 'Alex')
  await opFill(page, 'Last name', 'Smith')
  await opFill(page, 'Email', 'alex@example.com')
  await opPress(page, 'Enter')
})
