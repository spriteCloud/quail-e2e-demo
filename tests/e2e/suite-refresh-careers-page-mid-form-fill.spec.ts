/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Refresh careers page mid-form fill', async ({ page }) => {
  await opGoto(page, '/careers')
  await opFill(page, 'Name', 'Incomplete')
  await opGoto(page, '/careers')
  await opFill(page, 'Name', 'Fresh Start')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'dialog', 'Thank you for your application')
})
