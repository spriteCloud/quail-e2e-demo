/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Schedule a consultation by filling out the contact form', async ({ page }) => {
  await opGoto(page, '/contact')
  await opClick(page, 'combobox', 'Which service are you interested in')
  await opPress(page, 'ArrowDown')
  await opPress(page, 'Enter')
  await opFill(page, 'Your Name', 'Alex Rivera')
  await opFill(page, 'Your Email', 'alex@company.com')
  await opFill(page, 'Message', 'Interested in functional testing for our next release cycle.')
  await opClick(page, 'button', 'Send')
  await opSeen(page, 'heading', 'Thanks for getting in touch')
})
