/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Compare two case studies on mobile readiness', async ({ page }) => {
  await opGoto(page, '/work')
  await opClick(page, 'link', 'Oikocredit')
  await opClick(page, 'link', 'Home')
  await opGoto(page, '/case-study/citizenm')
  await opSeen(page, 'heading', 'citizenM')
})
