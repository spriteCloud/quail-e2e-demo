/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Comparing test automation with other services', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Test Automation')
  await opClick(page, 'link', 'Functional Testing')
  await opClick(page, 'link', 'Performance Testing')
})
