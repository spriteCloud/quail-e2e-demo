/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Navigate from homepage to careers, then press back repeatedly', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Careers')
  await opPress(page, 'Backspace')
  await opPress(page, 'Backspace')
  await opSeen(page, 'link', 'Careers')
})
