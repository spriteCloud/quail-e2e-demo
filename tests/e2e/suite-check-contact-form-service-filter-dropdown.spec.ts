/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Check contact form service filter dropdown', async ({ page }) => {
  await opGoto(page, '/contact')
  await opSeen(page, 'heading', 'Contact')
  await opSeen(page, 'combobox', 'Which service are you interested in')
  await opFill(page, 'Which service are you interested in', 'Test')
  await opSeen(page, 'option', 'Test Automation')
})
