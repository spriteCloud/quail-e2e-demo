/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open contact page in two tabs, switch, and submit', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Name', 'Adversarial User')
  await opFill(page, 'Email', 'test@example.com')
  await opFill(page, 'Message', 'Testing dual-tab submission')
  await opClick(page, 'button', 'Submit')
  await opClick(page, 'link', 'Contact')
  await opSeen(page, 'heading', 'Contact — spriteCloud')
})
