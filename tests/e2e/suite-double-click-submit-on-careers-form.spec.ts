/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Double-click submit on careers form', async ({ page }) => {
  await opGoto(page, '/careers')
  await opFill(page, 'Name', 'John Doe')
  await opFill(page, 'Email', 'john@example.com')
  await opClick(page, 'button', 'Submit')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'dialog', 'Form submission confirmation')
})
