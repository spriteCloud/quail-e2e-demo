/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Duplicate homepage tab mid-event registration', async ({ page }) => {
  await opGoto(page, '/event/barcelona-it-community-meetup-may-2026')
  await opClick(page, 'link', 'Register interest')
  await opFill(page, 'First Name', 'Adversarial')
  await opFill(page, 'Last Name', 'User')
  await opFill(page, 'Email', 'test@example.com')
  await opClick(page, 'button', 'Register')
})
