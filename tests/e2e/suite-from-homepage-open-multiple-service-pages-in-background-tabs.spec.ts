/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke From homepage, open multiple service pages in background tabs', async ({ page }) => {
  await opGoto(page, '/')
  await opPress(page, 'ControlOrMeta+Enter')
  await opClick(page, 'link', 'Test Automation')
  await opClick(page, 'link', 'Performance Testing')
  await opClick(page, 'link', 'Functional Testing')
  await opClick(page, 'link', 'Testing Services')
})
