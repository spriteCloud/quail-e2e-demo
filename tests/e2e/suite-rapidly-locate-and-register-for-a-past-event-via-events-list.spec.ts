/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Rapidly locate and register for a past event via events listing', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'QA-meetup-march-2025')
  await opClick(page, 'link', 'Register')
  await opSeen(page, 'heading', 'Event registration')
})
