/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Double-click contact form submit', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Your Name', 'John Doe')
  await opFill(page, 'Email', 'john@example.com')
  await opClick(page, 'button', 'Send')
  await opClick(page, 'button', 'Send')
  await opSeen(page, 'heading', 'Thank you')
})
