/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Switch tabs mid-contact-form fill to provoke state conflict', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Adversarial User')
  await opFill(page, 'Email', 'test@example.com')
  await opFill(page, 'Message', 'Testing tab-switch resilience')
  await opClick(page, 'link', 'Contact')
})
