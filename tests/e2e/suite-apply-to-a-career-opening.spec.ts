/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Apply to a career opening', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'button', 'Apply Now')
  await opFill(page, 'First Name', 'Alex')
  await opFill(page, 'Last Name', 'Rivera')
  await opFill(page, 'Email', 'alex.rivera@example.com')
  await opClick(page, 'button', 'Submit Application')
})
