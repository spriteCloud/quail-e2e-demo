/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke From contact form, open case study in new tab, then return and submit', async ({ page }) => {
  await opGoto(page, '/contact')
  await opClick(page, 'link', 'PostNL')
  await opPress(page, 'ControlOrMeta+w')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Name', 'Tabbing User')
  await opFill(page, 'Email', 'tab@user.test')
  await opPress(page, 'Enter')
})
