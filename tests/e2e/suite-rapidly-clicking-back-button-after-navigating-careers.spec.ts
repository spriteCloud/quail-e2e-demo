/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Rapidly clicking back button after navigating careers', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Company')
  await opClick(page, 'option', 'Careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opPress(page, 'Backspace')
  await opPress(page, 'Backspace')
  await opPress(page, 'Backspace')
  await opPress(page, 'Backspace')
  await opPress(page, 'Backspace')
  await opSeen(page, 'heading', 'Careers')
})
