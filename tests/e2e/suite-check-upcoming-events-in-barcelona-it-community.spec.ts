/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Check upcoming events in Barcelona IT community', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'Barcelona IT Community Meetup May 2026')
  await opClick(page, 'button', 'Register')
  await opFill(page, 'First Name', 'Alex')
  await opFill(page, 'Last Name', 'Rivera')
  await opFill(page, 'Email', 'alex.rivera@example.com')
  await opSeen(page, 'heading', 'Thank you for registering!')
})
