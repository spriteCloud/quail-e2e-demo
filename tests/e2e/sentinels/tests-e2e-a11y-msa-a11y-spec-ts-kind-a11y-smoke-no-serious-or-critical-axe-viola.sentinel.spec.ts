/**
 * Generated by quail from the bug-discovery ledger.
 *
 * Bug SENTINEL — this spec is intentionally `test.fail()`. It will
 * report "expected to fail" while the underlying bug stays unfixed,
 * and pivot to "unexpected pass" the moment the fix lands. That
 * pivot is your signal to remove the sentinel and graduate it to
 * a regular regression test.
 *
 *   - Bug:     @kind:a11y @smoke no serious or critical axe violations
 *   - Spec:    tests/e2e/a11y/msa.a11y.spec.ts
 *   - Symptom: Error: expect(received).toHaveLength(expected)
 *   - Severity: medium
 *   - First seen: 2026-06-20
 *
 * Filter: `npx playwright test --grep @kind:sentinel`
 */
import { test, expect } from '@playwright/test'

test.fail('@kind:sentinel @severity:medium @kind:a11y @smoke no serious or critical axe violations (intentionally fails until bug is fixed)', async ({ page }) => {
  // Sentinel test: intentionally fails to track unresolved accessibility bug
  // Until the bug is fixed, this test remains marked as `.fail()`;
  // once fixed, it will pass unexpectedly, signaling the test can be promoted.
  await page.goto('/')
  // Original symptom: assertion failure from Accessibility API validation
  expect(`Error: expect(received).toHaveLength(expected)`).toBe('') // fail if bug still present
})
