/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Explore API endpoints in Swagger UI', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Swagger UI')
  await opClick(page, 'button', 'GET /pets')
  await opSeen(page, 'heading', 'GET /pets')
})
