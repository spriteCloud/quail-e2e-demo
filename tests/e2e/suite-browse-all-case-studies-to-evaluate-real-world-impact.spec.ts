/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Browse all case studies to evaluate real-world impact', async ({ page }) => {
  await opGoto(page, '/work')
  await opClick(page, 'link', 'Oikocredit')
  await opClick(page, 'link', 'Back to case studies')
  await opClick(page, 'link', 'PostNL')
  await opClick(page, 'link', 'Back to case studies')
  await opClick(page, 'link', 'CitizenM')
  await opSeen(page, 'main', 'citizenM — Case Study — spriteCloud')
})
