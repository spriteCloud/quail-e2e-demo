/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill contact form with absurdly long inputs', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Functional Testing')
  await opFill(page, 'Name', 'ThisIsAVeryLongNameThatProbablyExceedsReasonableLengthButWeWantToStressTestTheInputConstraintsBecauseSometimesUsersDoWeirdThings')
  await opFill(page, 'Email', 'a@b.com')
  await opFill(page, 'Message', 'A long message that exceeds typical input expectations. ')
  await opClick(page, 'button', 'Send message')
})
