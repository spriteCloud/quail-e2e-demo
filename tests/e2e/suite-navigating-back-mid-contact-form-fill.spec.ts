/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Navigating back mid-contact-form fill', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Jane Doe')
  await opFill(page, 'Email', 'jane@example.com')
  await opClick(page, 'button', 'Which service are you interested in')
  await opClick(page, 'option', 'Test Automation')
  await opPress(page, 'Backspace')
  await opSeen(page, 'heading', 'Contact')
})
