/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke From homepage to case study in under 10 seconds', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'work')
  await opClick(page, 'link', 'PostNL')
  await opSeen(page, 'heading', 'PostNL')
})
