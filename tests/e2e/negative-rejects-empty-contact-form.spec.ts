/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:negative @smoke rejects empty contact form', async ({ page }) => {
  await opGoto(page, '/functional-testing')
  await opClick(page, 'link', 'Contact us')
  await opClick(page, 'button', 'Send')
  await opSeen(page, 'dialog', 'Please fill in all required fields')
})
