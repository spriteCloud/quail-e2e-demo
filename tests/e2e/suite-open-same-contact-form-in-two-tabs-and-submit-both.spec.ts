/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open same contact form in two tabs and submit both', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Tab 1 User')
  await opFill(page, 'Email', 'tab1@example.com')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'heading', 'Thank you')
  await opFill(page, 'Name', 'Tab 2 User')
  await opFill(page, 'Email', 'tab2@example.com')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'heading', 'Thank you')
})
