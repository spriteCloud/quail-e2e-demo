/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Refresh page mid-form completion and verify persisted state', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'HR Internship – Process & People')
  await opFill(page, 'Name', 'Risky User')
  await opFill(page, 'Email', 'risky@example.com')
  await opPress(page, 'F5')
})
