/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Overflow input in service-filter combobox', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Performance TestingPerformance TestingPerformance TestingPerformance TestingPerformance Testing')
  await opPress(page, 'Enter')
  await opSeen(page, 'combobox', 'Which service are you interested in')
})
