/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:negative @smoke rejects malformed email address', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Your name', 'Alex Smith')
  await opFill(page, 'Email address', 'not-an-email')
  await opFill(page, 'Phone number', '+1 (555) 123-4567')
  await opFill(page, 'Company name', 'Acme Corp')
  await opFill(page, 'What are you interested in?', 'Test Automation')
  await opFill(page, 'What\'s the situation?', 'Need help scaling our test suite for a new product launch.')
  await opClick(page, 'button', 'Send message')
  await opSeen(page, 'alert', 'Please enter a valid email address')
})
