/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Browse case study results without service context', async ({ page }) => {
  await opGoto(page, '/work')
  await opSeen(page, 'main', 'Work')
  await opClick(page, 'link', 'Oikocredit')
  await opSeen(page, 'main', 'Oikocredit')
})
