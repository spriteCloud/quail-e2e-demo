/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:composed @smoke Discover and sign up for newsletter', async ({ page }) => {
  await opGoto(page, '/')
  await opSeen(page, 'alert', 'You’re subscribed! Check your inbox.')
})
