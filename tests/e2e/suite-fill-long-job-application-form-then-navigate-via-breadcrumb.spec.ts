/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill long job application form then navigate via breadcrumb', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'button', 'Apply now')
  await opFill(page, 'First name', 'A')
  await opFill(page, 'Last name', 'Very Long Last Name That Is Definitely Not Intended But Tests Input Limits')
  await opFill(page, 'Email', 'a@b.c')
  await opFill(page, 'LinkedIn URL', 'https://www.linkedin.com/in/')
  await opClick(page, 'link', 'Back to careers')
  await opSeen(page, 'heading', 'Careers')
})
