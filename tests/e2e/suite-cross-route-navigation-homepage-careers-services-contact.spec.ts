/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Cross-route navigation: homepage → careers → services → contact', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Careers')
  await opClick(page, 'link', 'Testing Services')
  await opClick(page, 'link', 'Contact')
  await opSeen(page, 'heading', 'Contact')
})
