/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Jump from homepage to careers tab and filter by role', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'tab', 'Open positions')
  await opFill(page, 'Search by title', 'automation')
  await opSeen(page, 'heading', 'Test Automation Engineer')
})
