/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Quickly verify event registration form availability', async ({ page }) => {
  await opGoto(page, '/events')
  await opSeen(page, 'heading', 'Events & Meetups')
  await opSeen(page, 'dialog', 'Event registration')
  await opFill(page, 'Your email', 'test@example.com')
  await opPress(page, 'Tab')
})
