/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open contact in two tabs, fill and submit one then the other', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Tab One User')
  await opFill(page, 'Email', 'tab1@example.com')
  await opFill(page, 'Message', 'First tab')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Thank you')
})
