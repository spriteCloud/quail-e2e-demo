/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Navigate events → event details → submit event form → click browser back', async ({ page }) => {
  await opGoto(page, '/events')
  await opClick(page, 'link', 'QA Community Meetup April 2026')
  await opFill(page, 'Email', 'earlybird@test.com')
  await opClick(page, 'button', 'Register')
  await opPress(page, 'Backspace')
  await opSeen(page, 'link', 'QA Community Meetup April 2026')
})
