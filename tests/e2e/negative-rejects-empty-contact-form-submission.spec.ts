/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:negative @smoke rejects empty contact form submission', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opSeen(page, 'main', 'Test Automation Engineer')
  await opGoto(page, '/contact')
  await opClick(page, 'button', 'Send message')
})
