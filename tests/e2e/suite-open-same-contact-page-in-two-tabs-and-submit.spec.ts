/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Open same contact page in two tabs and submit', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Multi-tab User')
  await opFill(page, 'Email', 'tab@example.com')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'dialog', 'Thank you for your inquiry')
})
