/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Long-value spam in contact form followed by refresh', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'A very long name with many repeated characters: ')
  await opFill(page, 'Email', 'x')
  await opFill(page, 'Message', 'x')
})
