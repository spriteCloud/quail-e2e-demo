/* quail quality report
 * Weak / missing locators on this page:
 *   - no data-testid attributes found on this page (0 inputs, 4 anchors). Tests rely on text/role and break under copy edits.
 * Add data-testid to these elements for stable tests.
 */
/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:composed @smoke Explore spriteCloud\'s Test Automation page', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Test Automation')
  await opSeen(page, 'heading', 'Test More.Spend less.Ship faster.')
  await opSeen(page, 'heading', 'A framework your team can maintain. Not one that breaks in six months.')
  await opSeen(page, 'heading', 'Three layers. One framework where possible.')
  await opSeen(page, 'heading', 'Find out what automation would do for your team.')
})
