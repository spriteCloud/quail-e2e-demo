/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Back-button mid-event registration to verify state stability', async ({ page }) => {
  await opGoto(page, '/event/barcelona-it-community-meetup-may-2026')
  await opClick(page, 'button', 'Register now')
  await opFill(page, 'Name', 'Bob')
  await opPress(page, 'Tab')
  await opFill(page, 'Email', 'bob@example.com')
  await opPress(page, 'Backspace')
  await opClick(page, 'button', 'Register now')
  await opClick(page, 'link', 'Events')
  await opClick(page, 'button', 'Back')
  await opSeen(page, 'dialog', 'Register for Barcelona IT Community Meetup')
})
