/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Compare all services on the dedicated services page', async ({ page }) => {
  await opGoto(page, '/services')
  await opClick(page, 'heading', 'Testing Services')
  await opSeen(page, 'heading', 'Functional Testing')
  await opSeen(page, 'heading', 'Test Automation')
  await opSeen(page, 'heading', 'Performance Testing')
})
