/* quail quality report
 * Weak / missing locators on this page:
 *   - no data-testid attributes found on this page (0 inputs, 4 anchors). Tests rely on text/role and break under copy edits.
 * Add data-testid to these elements for stable tests.
 */
/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:composed @smoke Fill and submit contact form', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Your Name', 'Alex Johnson')
  await opFill(page, 'Email', 'alex.johnson@example.com')
  await opFill(page, 'Company', 'InnovateX')
  await opFill(page, 'Phone', '+1-555-123-4567')
  await opFill(page, 'What are you interested in?', 'QA automation services')
  await opFill(page, 'What\'s the situation?', 'Looking to improve test coverage for a new web app')
  await opClick(page, 'button', 'Send message')
})
