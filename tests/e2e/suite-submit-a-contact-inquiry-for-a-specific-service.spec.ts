/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Submit a contact inquiry for a specific service', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Your Name', 'Jordan Smith')
  await opFill(page, 'Your Email', 'jordan@company.com')
  await opFill(page, 'Message', 'We需要 automation support for our legacy app.')
  await opClick(page, 'button', 'Send')
})
