/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Attempt to register for a past event via event page', async ({ page }) => {
  await opGoto(page, '/event/qa-meetup-november-2025')
  await opClick(page, 'button', 'Register now')
  await opSeen(page, 'button', 'Register now')
})
