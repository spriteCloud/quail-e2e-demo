/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:coverage @smoke Visit homepage and verify main sections', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'button', 'Testing Services')
  await opClick(page, 'button', 'Company')
  await opSeen(page, 'heading', 'QA & Software Testing Consultancy')
})
