/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Cross-tab race: fill contact in tab A while tab B refreshes events page', async ({ page }) => {
  await opGoto(page, '/events')
  await opFill(page, 'Name', 'Multitasker')
  await opFill(page, 'Email', 'tabs@racer.net')
  await opClick(page, 'button', 'Submit')
  await opGoto(page, '/events')
  await opSeen(page, 'heading', 'Events & Meetups')
})
