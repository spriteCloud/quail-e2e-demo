/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Double-submit contact form to uncover race condition', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Functional Testing')
  await opFill(page, 'Name', 'Adversarial User')
  await opFill(page, 'Email', 'test@example.com')
  await opClick(page, 'button', 'Send message')
  await opClick(page, 'button', 'Send message')
  await opSeen(page, 'dialog', 'Message sent confirmation')
})
