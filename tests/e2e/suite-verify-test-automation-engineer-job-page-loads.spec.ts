/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Verify test-automation engineer job page loads', async ({ page }) => {
  await opGoto(page, '/vacancy/test-automation-engineer')
  await opSeen(page, 'main', 'Test Automation Engineer — Careers — spriteCloud')
})
