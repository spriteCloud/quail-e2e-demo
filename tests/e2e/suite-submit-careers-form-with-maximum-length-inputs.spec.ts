/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Submit careers form with maximum length inputs', async ({ page }) => {
  await opGoto(page, '/careers')
  await opFill(page, 'Name', 'A very long name that stretches well beyond typical expectations and might expose validation bugs or UI truncation issues')
  await opFill(page, 'Email', 'test@spritecloud.com')
  await opFill(page, 'CV / Resume (PDF)', '/path/to/very/long/fake.pdf')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'dialog', 'Thank you')
})
