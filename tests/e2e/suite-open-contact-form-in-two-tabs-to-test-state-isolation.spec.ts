/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open contact form in two tabs to test state isolation', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Tab A user')
  await opFill(page, 'Message', 'First tab content')
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Tab B user')
  await opSeen(page, 'textbox', 'Name')
})
