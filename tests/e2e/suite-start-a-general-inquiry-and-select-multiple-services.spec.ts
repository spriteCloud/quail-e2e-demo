/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Start a general inquiry and select multiple services', async ({ page }) => {
  await opGoto(page, '/contact')
  await opClick(page, 'link', 'Functional Testing')
  await opClick(page, 'link', 'Performance Testing')
  await opClick(page, 'button', 'Submit')
})
