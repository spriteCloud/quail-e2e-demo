/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill contact form, refresh mid-flow, then submit', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'John Doe')
  await opFill(page, 'Email', 'john@example.com')
  await opPress(page, 'Tab')
  await opPress(page, 'F5')
  await opFill(page, 'Name', 'Jane Smith')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Contact — spriteCloud')
})
