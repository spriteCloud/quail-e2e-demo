/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Skim events list for upcoming meetup dates', async ({ page }) => {
  await opGoto(page, '/events')
  await opSeen(page, 'main', 'Events & Meetups')
  await opSeen(page, 'heading', 'Upcoming Events')
  await opSeen(page, 'link', 'QA Community Meetup April 2026')
})
