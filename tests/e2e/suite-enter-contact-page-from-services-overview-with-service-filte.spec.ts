/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Enter contact page from services overview with service filter', async ({ page }) => {
  await opGoto(page, '/services')
  await opClick(page, 'link', 'Contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opSeen(page, 'dialog', 'Contact form (filtered)')
})
