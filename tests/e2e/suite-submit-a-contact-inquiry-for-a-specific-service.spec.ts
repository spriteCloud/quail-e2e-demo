/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Submit a contact inquiry for a specific service', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Name', 'Alex Client')
  await opFill(page, 'Email', 'alex@example.com')
  await opFill(page, 'Message', 'We need automation support for a large-scale retail app.')
  await opPress(page, 'Enter')
  await opSeen(page, 'main', 'Contact')
})
