/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Filtering by service interest in contact form', async ({ page }) => {
  await opGoto(page, '/contact')
  await opClick(page, 'combobox', 'Which service are you interested in')
  await opClick(page, 'option', 'Test Automation')
  await opFill(page, 'Your message', 'We need scalable test automation for our CI/CD pipelines.')
  await opClick(page, 'button', 'Send message')
  await opSeen(page, 'heading', 'Thank You')
})
