/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Back-button mid-career form', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opClick(page, 'button', 'Apply')
  await opFill(page, 'First name', 'Jane')
  await opClick(page, 'link', 'Back to Careers')
  await opSeen(page, 'heading', 'Careers')
})
