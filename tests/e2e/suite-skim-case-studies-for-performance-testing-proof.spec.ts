/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Skim case studies for performance testing proof', async ({ page }) => {
  await opGoto(page, '/performance-testing')
  await opClick(page, 'link', 'eCommerce platform')
  await opSeen(page, 'heading', 'Performance Testing')
  await opClick(page, 'link', 'Oikocredit')
  await opSeen(page, 'heading', 'Oikocredit')
})
