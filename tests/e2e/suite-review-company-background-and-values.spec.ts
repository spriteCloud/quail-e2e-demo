/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Review company background and values', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Company')
  await opClick(page, 'link', 'About')
  await opClick(page, 'link', 'Testing Services')
  await opSeen(page, 'heading', 'Functional Testing')
  await opSeen(page, 'heading', 'Test Automation')
  await opSeen(page, 'heading', 'Performance Testing')
})
