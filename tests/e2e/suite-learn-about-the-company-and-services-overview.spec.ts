/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Learn about the company and services overview', async ({ page }) => {
  await opGoto(page, '/about')
  await opClick(page, 'link', 'Working at spriteCloud')
  await opSeen(page, 'heading', 'About spriteCloud')
  await opClick(page, 'link', 'Testing Services')
  await opClick(page, 'link', 'Test Automation')
})
