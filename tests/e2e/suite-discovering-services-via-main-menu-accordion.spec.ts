/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Discovering services via main menu accordion', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Testing Services')
  await opClick(page, 'option', 'Functional Testing')
  await opClick(page, 'option', 'Test Automation')
  await opClick(page, 'option', 'Performance Testing')
  await opSeen(page, 'heading', 'Testing Services')
})
