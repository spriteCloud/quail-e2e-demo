/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Check case studies in niche verticals and explore contact', async ({ page }) => {
  await opGoto(page, '/case-study/giftshift')
  await opSeen(page, 'heading', 'GiftShift')
  await opClick(page, 'link', 'Case Study')
  await opClick(page, 'link', 'Contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Name', 'Alex')
  await opFill(page, 'Email', 'alex@example.com')
  await opFill(page, 'Message', 'Interested in automation for agentic systems')
  await opClick(page, 'button', 'Send')
})
