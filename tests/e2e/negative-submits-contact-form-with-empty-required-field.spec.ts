/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:negative @smoke submits contact form with empty required field', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', '')
  await opPress(page, 'Enter')
  await opSeen(page, 'alert', 'Please fill out this field')
})
