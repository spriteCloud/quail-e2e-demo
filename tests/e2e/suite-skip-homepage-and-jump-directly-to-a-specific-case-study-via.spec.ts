/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Skip homepage and jump directly to a specific case study via search', async ({ page }) => {
  await opFill(page, 'Search', 'postnl')
  await opPress(page, 'Enter')
  await opClick(page, 'link', 'PostNL — Case Study')
  await opSeen(page, 'heading', 'PostNL — Case Study')
})
