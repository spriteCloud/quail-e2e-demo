/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Rapid double-clicks on \'Submit\' button in careers form', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'HR Internship')
  await opFill(page, 'First Name', 'Dana')
  await opFill(page, 'Last Name', 'Lee')
  await opFill(page, 'Email', 'dana.lee@example.net')
  await opClick(page, 'button', 'Submit')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'heading', 'Thank you for your application')
})
