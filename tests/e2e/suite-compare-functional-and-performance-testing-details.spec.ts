/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Compare functional and performance testing details', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Functional Testing')
  await opClick(page, 'link', 'Performance Testing')
  await opClick(page, 'link', 'Test your application under expected and peak loads')
  await opClick(page, 'link', 'Functional Testing')
  await opClick(page, 'link', 'Functional Testing')
  await opSeen(page, 'heading', 'Functional Testing')
})
