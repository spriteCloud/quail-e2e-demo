/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Browse Agentic Testing page to evaluate innovation', async ({ page }) => {
  await opGoto(page, '/agentic-testing')
  await opSeen(page, 'heading', 'Agentic Testing')
  await opClick(page, 'link', 'Test Automation')
  await opSeen(page, 'heading', 'Test Automation')
})
