/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Survey real-world service usage in case studies', async ({ page }) => {
  await opGoto(page, '/case-study/e-commerce-performance')
  await opSeen(page, 'heading', 'eCommerce platform')
  await opClick(page, 'link', 'Test Automation')
  await opSeen(page, 'heading', 'Test Automation')
  await opClick(page, 'link', 'Performance Testing')
  await opSeen(page, 'heading', 'Performance Testing')
})
