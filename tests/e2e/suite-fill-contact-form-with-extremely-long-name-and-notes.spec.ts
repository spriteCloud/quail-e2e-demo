/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Fill contact form with extremely long name and notes', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'AlexandertheGreatWhoseNameMustBeSpelledOutInFullBecauseReasons')
  await opFill(page, 'Email', 'alex@test.com')
  await opFill(page, 'Message', 'Long message with many details about edge cases, boundary values, and regression risks.')
  await opClick(page, 'button', 'Send inquiry')
})
