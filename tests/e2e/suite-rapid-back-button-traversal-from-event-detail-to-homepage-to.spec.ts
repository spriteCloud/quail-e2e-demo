/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Rapid back-button traversal from event detail to homepage to career', async ({ page }) => {
  await opGoto(page, '/event/qa-meetup-march-2025')
  await opPress(page, 'Backspace')
  await opPress(page, 'Backspace')
  await opClick(page, 'link', 'Careers')
})
