/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill contact form using long service filter then back-button', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Functional Testing, Test Automation, Performance Testing, Cybersecurity & Penetration Testing, Agentic Testing')
  await opClick(page, 'button', 'Submit')
  await opClick(page, 'button', 'Back')
  await opSeen(page, 'heading', 'Contact')
})
