/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Navigate events archive by refreshing past event page', async ({ page }) => {
  await opGoto(page, '/event/qa-meetup-march-2025')
  await opClick(page, 'link', 'Events & Meetups')
  await opClick(page, 'link', 'Events & Meetups')
  await opPress(page, 'Tab')
  await opPress(page, 'Tab')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Events & Meetups')
})
