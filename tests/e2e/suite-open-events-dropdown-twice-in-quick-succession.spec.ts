/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open events dropdown twice in quick succession', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Testing Services')
  await opClick(page, 'menuitem', 'Events & Meetups')
  await opClick(page, 'menuitem', 'Testing Services')
  await opSeen(page, 'heading', 'Events & Meetups')
})
