/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:negative @smoke submits empty required contact form fields', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Email', '')
  await opClick(page, 'button', 'Send message')
  await opSeen(page, 'dialog', 'Please fill in all required fields')
})
