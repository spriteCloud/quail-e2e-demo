/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Navigate from homepage to About and work page', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'About')
  await opClick(page, 'link', 'Work')
  await opSeen(page, 'heading', 'Work')
})
