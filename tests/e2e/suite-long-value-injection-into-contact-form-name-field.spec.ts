/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Long-value injection into contact form name field', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Name', 'This is an extremely long name that exceeds typical input validation limits and may test server-side truncation or error handling in the form submission process—hope it works!')
  await opFill(page, 'Email', 'valid@test.com')
  await opFill(page, 'Message', 'Testing boundary cases.')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'main', 'Contact')
})
