# Generated by quail.
#
# Gherkin contract for the discover journey on https://www.spritecloud.com/.
#
# This .feature file is EXECUTED by playwright-bdd at test time.
# Step definitions live in `tests/e2e/steps/quail.steps.ts` and bind
# each Given/When/Then to the helper verbs in `tests/e2e/lib/steps.ts`.
#
# Tag filtering:
#   npx playwright test --grep @priority:critical
#   npx playwright test --grep @journey:discover
#   npx playwright test --grep @smoke

Feature: Spritecloud — discover journey
  As a visitor of https://www.spritecloud.com/
  I want to complete the discover flow
  So that the page delivers on its user goal

  @journey:discover @priority:standard @smoke
  Scenario: discover journey reaches its terminal page
    Given I open the landing page
    And the page title contains "QA & Software Testing Consultancy — spriteCloud"
    When I navigate directly to "/services"
    Then I see the heading "Not a service list.
A diagnostic."
    And the page title contains "Testing Services — spriteCloud"

  @journey:discover @priority:standard @kind:empty-state
  Scenario: discover — page renders the landing list
    Given I open the landing page
    Then the page has at least 1 items

  @journey:discover @priority:standard @kind:resume
  Scenario: discover — deep-link to the terminal page renders correctly
    Given I open the page "/services"
    Then I see the heading "Not a service list.
A diagnostic."

  @journey:discover @priority:standard @kind:back-button
  Scenario: discover — back button after navigation returns to landing
    Given I open the landing page
    When I click the link to "/services"
    When I go back in the browser history
