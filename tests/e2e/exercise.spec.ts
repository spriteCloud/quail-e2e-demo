/* quail quality report
 * Weak / missing locators on this page:
 *   - no data-testid attributes found on this page (0 inputs, 4 anchors). Tests rely on text/role and break under copy edits.
 * Add data-testid to these elements for stable tests.
 */
/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:composed @smoke Explore spriteCloud services and contact for consultation', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'heading', 'Quality breaks in predictable places. We know where.')
  await opClick(page, 'heading', 'Ready to talk?')
  await opClick(page, 'button', 'Let\'s Talk')
  await opFill(page, 'Name', 'Alex Johnson')
  await opFill(page, 'Email', 'alex.johnson@example.com')
  await opFill(page, 'Message', 'I\'m interested in your QA consultancy services.')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'alert', 'Thank you for your message. We will be in touch soon.')
})
