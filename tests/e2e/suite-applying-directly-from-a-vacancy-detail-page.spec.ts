/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Applying directly from a vacancy detail page', async ({ page }) => {
  await opGoto(page, '/vacancy/test-automation-engineer')
  await opClick(page, 'link', 'Apply now')
  await opSeen(page, 'dialog', 'Job application')
})
