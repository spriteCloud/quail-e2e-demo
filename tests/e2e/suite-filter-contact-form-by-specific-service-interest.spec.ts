/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Filter contact form by specific service interest', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Performance Testing')
  await opFill(page, 'Name', 'Jordan Smith')
  await opFill(page, 'Email', 'jordan@example.com')
  await opFill(page, 'Message', 'Need help optimizing our test suite.')
  await opClick(page, 'button', 'Send message')
})
