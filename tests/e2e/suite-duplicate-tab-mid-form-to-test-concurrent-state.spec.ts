/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Duplicate tab mid-form to test concurrent state', async ({ page }) => {
  await opGoto(page, '/careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opFill(page, 'First Name', 'Alex')
  await opFill(page, 'Last Name', 'Rivera')
  await opFill(page, 'Email', 'alex.rivera@test.com')
  await opFill(page, 'LinkedIn URL', 'https://linkedin.com/in/alexrivera')
  await opFill(page, 'Portfolio URL', 'https://alexrivera.dev')
})
