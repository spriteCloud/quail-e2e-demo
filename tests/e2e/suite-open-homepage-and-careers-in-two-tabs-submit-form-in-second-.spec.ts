/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Open homepage and careers in two tabs, submit form in second tab', async ({ page }) => {
  await opGoto(page, '/')
  await opGoto(page, '/careers')
  await opFill(page, 'First Name', 'Alex')
  await opFill(page, 'Last Name', 'Smith')
  await opFill(page, 'Email', 'alex.smith@example.com')
  await opClick(page, 'button', 'Send application')
})
