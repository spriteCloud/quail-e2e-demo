/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill contact form with extremely long value to test validation', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Test Automation')
  await opFill(page, 'Name', 'A very long name that exceeds typical input field limits and may expose truncation bugs in the backend or frontend validation')
  await opFill(page, 'Email', 'x@x.x')
  await opFill(page, 'Message', 'This is a very long message designed to exceed expected field lengths and test how the system handles boundary conditions in input validation. It contains repeated filler text to push past common server-side constraints. ')
  await opClick(page, 'button', 'Send message')
  await opSeen(page, 'dialog', 'Message sent confirmation')
})
