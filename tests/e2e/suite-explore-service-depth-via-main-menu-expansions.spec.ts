/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Explore service depth via main menu expansions', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Testing Services')
  await opSeen(page, 'option', 'Functional Testing')
  await opClick(page, 'option', 'Test Automation')
  await opSeen(page, 'heading', 'Test Automation')
})
