/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Double-click submit button on event registration', async ({ page }) => {
  await opGoto(page, '/event/barcelona-it-community-meetup-may-2026')
  await opClick(page, 'button', 'Register')
  await opFill(page, 'First name', 'Bouncer')
  await opFill(page, 'Last name', 'Twice')
  await opFill(page, 'Email', 'bouncer@example.com')
  await opClick(page, 'button', 'Register')
  await opPress(page, 'Enter')
  await opSeen(page, 'dialog', 'Registration confirmed')
})
