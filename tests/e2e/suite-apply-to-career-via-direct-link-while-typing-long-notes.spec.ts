/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Apply to career via direct link while typing long notes', async ({ page }) => {
  await opGoto(page, '/vacancy/test-automation-engineer')
  await opFill(page, 'First Name', 'Alex')
  await opFill(page, 'Last Name', 'Runner')
  await opFill(page, 'Email', 'alex@example.com')
  await opFill(page, 'LinkedIn profile', 'https://www.linkedin.com/in/alexrunner')
  await opFill(page, 'Portfolio or GitHub', 'https://github.com/alexrunner')
  await opFill(page, 'Cover letter', 'I have strong experience in API and UI automation with Playwright, Cypress, and TestNG, and I\'m passionate about shifting-left testing and AI-augmented QA. I’m excited about spriteCloud’s mission and would love to contribute to innovation in test automation.')
  await opClick(page, 'button', 'Apply')
})
