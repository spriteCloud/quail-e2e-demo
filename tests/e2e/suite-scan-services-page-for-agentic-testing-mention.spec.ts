/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Scan services page for agentic testing mention', async ({ page }) => {
  await opGoto(page, '/services')
  await opSeen(page, 'heading', 'Testing Services')
  await opClick(page, 'heading', 'Agentic Testing')
})
