/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Cross-tab concurrent form submission', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Tab 1 User')
  await opFill(page, 'Email', 'tab1@spritecloud.test')
  await opFill(page, 'Message', 'First tab intent')
  await opClick(page, 'button', 'Send message')
  await opFill(page, 'Name', 'Tab 2 User')
  await opFill(page, 'Email', 'tab2@spritecloud.test')
  await opFill(page, 'Message', 'Second tab intent')
  await opClick(page, 'button', 'Send message')
  await opSeen(page, 'main', 'Contact')
})
