/* quail quality report
 * Weak / missing locators on this page:
 *   - no data-testid attributes found on this page (0 inputs, 4 anchors). Tests rely on text/role and break under copy edits.
 * Add data-testid to these elements for stable tests.
 */
/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:composed @smoke Submit contact form', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'First name', 'Alex')
  await opFill(page, 'Last name', 'Morgan')
  await opFill(page, 'Email', 'alex.morgan@example.com')
  await opFill(page, 'Company', 'TechGlobal')
  await opFill(page, 'What are you interested in?', 'QA automation')
  await opFill(page, 'What\'s the situation?', 'We need a test strategy for a new product launch.')
  await opClick(page, 'button', 'Send message')
})
