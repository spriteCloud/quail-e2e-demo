/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Scan homepage for key differentiators', async ({ page }) => {
  await opGoto(page, '/')
  await opSeen(page, 'heading', 'QA & Software Testing Consultancy')
  await opSeen(page, 'main', 'spriteCloud')
  await opSeen(page, 'heading', 'End-to-end QA and test automation that scales with your business')
  await opSeen(page, 'link', 'How it works')
})
