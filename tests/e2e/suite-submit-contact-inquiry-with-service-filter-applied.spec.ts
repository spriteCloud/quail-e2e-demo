/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Submit contact inquiry with service filter applied', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Alex Tester')
  await opFill(page, 'Email', 'alex@example.com')
  await opFill(page, 'Company', 'TechCo')
  await opFill(page, 'Message', 'Interested in performance testing consultation')
  await opFill(page, 'Which service are you interested in', 'Performance Testing')
  await opClick(page, 'button', 'Submit')
})
