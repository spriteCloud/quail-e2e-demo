/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Discover company values and team through About page', async ({ page }) => {
  await opGoto(page, '/about')
  await opClick(page, 'link', 'About')
  await opClick(page, 'link', 'Testing Services')
  await opSeen(page, 'link', 'Functional Testing')
})
