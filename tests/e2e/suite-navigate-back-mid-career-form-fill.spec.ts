/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Navigate back mid-career form fill', async ({ page }) => {
  await opGoto(page, '/careers')
  await opFill(page, 'Name', 'Jane Smith')
  await opFill(page, 'Email', 'jane@example.com')
  await opClick(page, 'button', 'Upload CV')
  await opClick(page, 'link', 'Back')
  await opClick(page, 'link', 'Back')
  await opSeen(page, 'heading', 'Careers')
})
