/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Evaluate company credibility via careers and contact', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'menuitem', 'Careers')
  await opClick(page, 'link', 'Test Automation Engineer')
  await opSeen(page, 'main', 'Test Automation Engineer')
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Jordan')
  await opFill(page, 'Email', 'jordan@company.com')
  await opFill(page, 'Message', 'Interests in performance testing partnership')
  await opClick(page, 'button', 'Send message')
})
