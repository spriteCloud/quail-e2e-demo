/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Read two different case studies back-to-back', async ({ page }) => {
  await opGoto(page, '/work')
  await opClick(page, 'link', 'Oikocredit — Case Study — spriteCloud')
  await opClick(page, 'link', 'PostNL — Case Study — spriteCloud')
  await opSeen(page, 'heading', 'PostNL — Case Study — spriteCloud')
})
