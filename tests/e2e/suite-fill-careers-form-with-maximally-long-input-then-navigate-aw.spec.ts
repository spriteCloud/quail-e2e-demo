/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill careers form with maximally long input then navigate away', async ({ page }) => {
  await opGoto(page, '/careers')
  await opFill(page, 'Name', 'A very long name that could exceed typical length limits in form fields for testing purposes without crashing')
  await opFill(page, 'Email', 'a_very_long_email_address_that_might_cause_issues_at_some_servers@example.com')
  await opPress(page, 'Tab')
  await opPress(page, 'Tab')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Thank you! We\'ll be in touch shortly')
  await opClick(page, 'link', 'Home')
})
