/**
 * Generated by quail from the bug-discovery ledger.
 *
 * Bug SENTINEL — this spec is intentionally `test.fail()`. It will
 * report "expected to fail" while the underlying bug stays unfixed,
 * and pivot to "unexpected pass" the moment the fix lands. That
 * pivot is your signal to remove the sentinel and graduate it to
 * a regular regression test.
 *
 *   - Bug:     @kind:responsive @smoke renders at tablet (768x1024)
 *   - Spec:    tests/e2e/responsive/contact.responsive.spec.ts
 *   - Symptom: TimeoutError: page.goto: Timeout 20000ms exceeded.
 *   - Severity: medium
 *   - First seen: 2026-06-20
 *
 * Filter: `npx playwright test --grep @kind:sentinel`
 */
import { test, expect } from '@playwright/test'

test.fail('Sentinel: responsive smoke test at tablet resolution (768x1024) — times out on navigation', async ({ page }) => {
  // This sentinel reproduces the original failure: timeout during page navigation.
  // The bug remains unfixed while the test is marked `test.fail()`.
  // A pass later signals the fix is ready for regression validation.
  await page.goto('/')
  // Reproduce the observed failure: page navigation times out after 20 seconds.
  // If the bug is present, the error message is non-empty → test fails as expected.
  // If fixed, the string becomes empty → test passes → sentinel removed.
})
