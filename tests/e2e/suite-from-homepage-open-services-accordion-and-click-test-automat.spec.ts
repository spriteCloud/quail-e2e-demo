/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke From homepage, open services accordion and click Test Automation', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'button', 'Testing Services')
  await opClick(page, 'link', 'Test Automation')
})
