/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Scan company info on about page', async ({ page }) => {
  await opGoto(page, '/about')
  await opSeen(page, 'heading', 'About')
  await opSeen(page, 'link', 'Testing Services')
  await opSeen(page, 'link', 'Function')
  await opSeen(page, 'heading', 'Mission')
})
