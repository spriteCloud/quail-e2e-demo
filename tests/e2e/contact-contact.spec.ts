/* quail quality report
 * Weak / missing locators on this page:
 *   - no data-testid attributes found on this page (0 inputs, 4 anchors). Tests rely on text/role and break under copy edits.
 * Add data-testid to these elements for stable tests.
 */
/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:composed @smoke Send a contact inquiry', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Full name', 'Alex Smith')
  await opFill(page, 'Email address', 'alex@example.com')
  await opFill(page, 'Phone number', '+1 (555) 123-4567')
  await opFill(page, 'Company name', 'Acme Corp')
  await opFill(page, 'What are you interested in?', 'QA automation services')
  await opFill(page, 'What\'s the situation?', 'We need help improving our test coverage for a legacy Java app.')
  await opClick(page, 'button', 'Send inquiry')
})
