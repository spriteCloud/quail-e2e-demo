/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Fill careers form with maximum-length field then press Tab and Enter', async ({ page }) => {
  await opGoto(page, '/careers')
  await opFill(page, 'Name', 'Name')
  await opFill(page, 'Email', 'test@example.com')
  await opFill(page, 'Phone', '1234567890')
  await opFill(page, 'Message', 'This is a very long message that intentionally exceeds typical input limits to see if validation handles overflow gracefully without crashing or truncating unexpectedly.')
  await opPress(page, 'Tab')
  await opPress(page, 'Enter')
  await opSeen(page, 'dialog', 'Thank you for your application')
})
