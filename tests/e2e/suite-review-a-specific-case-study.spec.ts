/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Review a specific case study', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'Oikocredit')
  await opClick(page, 'link', 'Oikocredit — Case Study')
  await opSeen(page, 'heading', 'Oikocredit')
})
