/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Back-button navigation after form fill on careers page', async ({ page }) => {
  await opGoto(page, '/careers')
  await opFill(page, 'Full Name', 'John Doe')
  await opFill(page, 'Email', 'john@example.com')
  await opClick(page, 'button', 'Submit')
  await opPress(page, 'BackSpace')
  await opPress(page, 'Enter')
  await opSeen(page, 'heading', 'Careers')
})
