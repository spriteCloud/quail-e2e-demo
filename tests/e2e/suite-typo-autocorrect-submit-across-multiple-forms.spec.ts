/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from '../prims'

test('@journey:suite @smoke Typo → autocorrect → submit across multiple forms', async ({ page }) => {
  await opGoto(page, '/contact')
  await opFill(page, 'Name', 'Jhn Doe')
  await opFill(page, 'Email', 'jhn@example.con')
  await opPress(page, 'Tab')
  await opPress(page, 'Tab')
  await opPress(page, 'Tab')
  await opFill(page, 'Name', 'Jane Doe')
  await opFill(page, 'Email', 'jane@example.com')
  await opClick(page, 'button', 'Submit')
  await opSeen(page, 'dialog', 'Thank you for your message')
})
