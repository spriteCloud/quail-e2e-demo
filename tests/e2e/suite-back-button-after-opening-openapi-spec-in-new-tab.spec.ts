/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Back button after opening OpenAPI spec in new tab', async ({ page }) => {
  await opGoto(page, '/')
  await opClick(page, 'link', 'openapi.json')
  await opPress(page, 'ControlOrMeta+w')
  await opPress(page, 'Shift+Tab')
  await opPress(page, 'Enter')
})
