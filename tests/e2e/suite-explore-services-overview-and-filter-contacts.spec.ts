/** Generated by quail. */
import { test } from '@playwright/test'
import { opGoto, opClick, opFill, opPress, opSeen } from './prims'

test('@journey:suite @smoke Explore services overview and filter contacts', async ({ page }) => {
  await opGoto(page, '/services')
  await opClick(page, 'link', 'Testing Services')
  await opGoto(page, '/contact')
  await opFill(page, 'Which service are you interested in', 'Functional Testing')
  await opSeen(page, 'combobox', 'Which service are you interested in')
})
